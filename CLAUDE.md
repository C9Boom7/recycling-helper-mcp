# 재활용 척척 MCP — 작업 규칙

도구 사용 우선순위와 PlayMCP 배경은 [AGENTS.md](AGENTS.md)를 따른다.

## Git 워크플로 (반드시 지킬 것)

- **main 머지와 main 직접 push는 금지한다.** GitHub Actions에서 실행 중이더라도 예외가 없다.
  main 갱신은 PlayMCP 본선 서버 재배포를 그대로 트리거하므로, 머지 타이밍은 저장소 주인이 직접 정한다.
- 작업은 브랜치에서 하고 PR로 올린다. 리뷰 지적을 반영할 때도 해당 PR의 브랜치에만 커밋한다.
- 커밋 author는 저장소 주인(c9boom7)으로 두고, 에이전트 기여는 `Co-Authored-By`로 남긴다.

## 검증

- 코드나 데이터를 고쳤으면 `pnpm local:test`가 통과해야 한다
  (타입 검사 2벌 + 데이터 검증 + 지역 매칭 + MCP 스모크).
- 데이터 파일만 고쳤어도 스모크까지 돌린다. 툴 스키마와 데이터가 함께 깨지는 사고가 있었다.

## 문서

- 새로 쓰는 한국어 문장은 번역투를 피하고 원어민이 쓸 법한 문장으로 쓴다.
- PRD와 진행 상황은 `docs/prd/` 아래에 둔다.
