# Kakao Tools Preview 발화 테스트 결과 — 2026-08-14

## 실행 환경

- MCP: 임시 등록본 `재활용척척` (식별자 `recycleHelperV2`)
- Endpoint: `https://recycling-helper-mcp-kakaotools.playmcp-endpoint.kakaocloud.io/mcp` (items 272, Phase 0~3 반영)
- 도구함: 이 MCP만 활성화. 운영 등록본(구버전 서버)은 미적용
- 발화 입력은 사용자가 직접 수행 (교차 출처 iframe이라 자동화 불가 — `playmcp-in-kc.md` 참고)

## 결과

| id | 발화 | 호출된 툴 | 위젯 | 판정 | 메모 |
| --- | --- | --- | --- | --- | --- |
| D1 | 기름 묻은 피자박스 어떻게 버려? | get_disposal_steps | O | pass | 카드 제목 "기름 묻은 피자박스", 캡션 "재활용/일반쓰레기", 3단계 + 주의 + 근거. 카드 아래 ChatGPT 요약 1줄 |
| T1 | 패트병 라벨 떼야돼? | get_disposal_steps | O | pass | 자모 오타 매칭으로 "페트병" 카드. ChatGPT가 "응, 떼야 해!"로 질문에 직접 답함 |
| A1 | 컵 어떻게 버려? | 판별 불가 | X | 보류 | "유리컵/플라스틱컵/종이컵 중 어떤 거야?"로 되물음. 되묻기 동작 자체는 의도대로 |
| A1-1 | (이어서) 기름묻은 종이컵 | get_disposal_steps | O | pass | "종이컵" 카드, 캡션 "재활용/일반쓰레기", 4단계 |
| R3 | 강남구 사는데 침대 어떻게 버려? | get_region_disposal_info | X | **fail (오라우팅)** | 답 내용은 정확하나 `get_disposal_steps`가 아닌 지역 툴로 갔다. 아래 분석 참고 |
| N1 | 오늘 저녁 뭐 먹지 | (없음) | X | pass | "제육볶음 + 계란찜 + 밥" — 툴 미호출. 과호출 0 |
| U1 | 실리콘 뒤집개 버리는 법 | get_disposal_steps | O | pass | "뒤집개" 카드. 주의에 "실리콘·나일론 뒤집개는 재활용이 안 되므로 일반쓰레기" — 실리콘 조건을 정확히 짚음 |

| D2 | 깨진 유리컵 버리는 법 알려줘 | get_disposal_steps | O | pass | 캡션 "일반쓰레기/불연성", 4단계. **주의 첫 줄이 "수거 작업자가 다치지 않도록 날카로운 부분을 반드시 감싸세요"** — Phase 3 R2의 안전 문구 우선 정렬이 동작 |
| D3 | 폐건전지 어디다 버려? | get_disposal_steps | O | pass | "건전지" 카드, 특수/유해폐기물, 화재 위험 주의 |
| D4 | 보조배터리는 어디에 배출해? | get_disposal_steps | O | pass | 부풂·파손·누액 분기까지 4단계. 주의 2줄(리튬이온 충격·압력·고온, 파손품 억지 투입 금지) |
| D5 | 아이스팩 어떻게 버리는 게 맞아? | 판별 불가 | X | 보류 | 카드 없이 종합 텍스트에만 포함. 내용은 데이터와 일치(물/젤 분기) |
| J1~J4 | 판단형 4건 | 판별 불가 | X | 재측정 | 한 메시지에 4개를 붙여 보내 표로 종합 답변. 아래 참고 |

의도 발화 중 위젯으로 호출이 확정된 것은 7건. 비의도 발화 과호출 0. 오답·무용답 0건.

## 측정 환경을 ChatGPT 본체로 옮겼다 (2026-08-14 저녁)

Preview는 채팅 영역이 교차 출처 iframe이라 자동화가 불가능했다. PlayMCP 도구함을 **ChatGPT 본체(chatgpt.com)에 커넥터로 연결**해
Claude가 직접 발화를 넣고 결과를 읽는 방식으로 바꿨다. ChatGPT Plus가 필요하다(커넥터는 유료 전용).

이 환경의 결정적 장점은 **호출된 툴 이름·인자·응답 원문이 화면에 그대로 노출된다**는 것이다. "도구 요청 완료"를 펼치면
`Recycle Helper V2 get disposal steps` / `{itemName: "아이스팩"}` / 위젯 JSON 전문까지 보인다.
Preview에서 끝내 풀지 못한 "텍스트 응답 툴은 호출 여부를 알 수 없다" 문제가 사라졌다.

한계와 주의점:

- **호스트가 ChatGPT for Kakao가 아니라 ChatGPT 본체다.** 모델 성향이 완전히 같지 않다.
- **메시지마다 `+` 메뉴에서 커넥터를 선택해야 한다.** 이 선택 행위 자체가 "이 툴을 쓰라"는 압력으로 작용해
  **비의도 발화의 과호출이 과대평가된다.** 실제로 N1은 Preview에서 미호출, 본체에서 호출로 갈렸다.
- ChatGPT 본체는 대화 간 메모리가 있어, 사용자가 말하지 않은 지역("강남구")을 답변에 끌어오는 일이 있었다. Kakao Tools에는 없는 요인이다.
- 위젯은 카드로 렌더링되지 않는다(카카오 전용). JSON 구조 확인까지만 가능하다.

따라서 **툴 호출·라우팅은 본체에서 자동 측정하고, 과호출 판정과 위젯 렌더링은 Preview 육안 확인으로 확정**한다.

### 본체 측정 결과 (수정 전 description 기준)

| id | 발화 | 호출된 툴 | 판정 |
| --- | --- | --- | --- |
| D1 | 기름 묻은 피자박스 어떻게 버려? | `get_disposal_steps` | pass |
| D5 | 아이스팩 어떻게 버리는 게 맞아? | `get_disposal_steps` | pass |
| J1 | 스티로폼 재활용 돼? | `classify_waste_item` | pass |
| J2 | 영수증은 종이인데 왜 재활용 안 돼? | 호출됨 | pass |
| J3 | 프라이팬 분리수거 되나? | 호출됨 | pass |
| J4 | 도자기 그릇도 유리류로 버리면 돼? | 호출됨 | pass |
| T2 | 스치로폼 버리는법 | 호출됨 | pass (자모 매칭) |
| U2 | 캐핑용 가스버너 버리려면 어떻게 해? | 호출됨 | pass (오타+미등재 폴백) |
| A1 | 컵 어떻게 버려? | 호출됨 | pass — 답변에 "컵라면 용기" 등장 |
| M1 | 이사가서 침대랑 화분이랑 옷 버려야 해 | `make_cleanup_plan` | pass |
| R3 | 강남구 사는데 침대 어떻게 버려? | `get_region_disposal_info` | **fail (오라우팅)** |
| N1 | 오늘 저녁 뭐 먹지 | 호출됨 | fail (과호출 — 위 주의점 참고) |

**의도 발화 11건 중 10건 호출 + 정확, 1건 오라우팅.**

두 가지가 확인됐다.

1. **판단형은 툴을 제대로 쓴다.** Preview에서 표로 종합 답변이 나온 건 4개를 한 메시지에 붙여 보낸 탓이었다. 하나씩 보내니 4건 모두 호출된다.
2. **R3 오라우팅이 본체에서도 동일하게 재현된다.** Preview와 같은 `get_region_disposal_info` 선택, 수수료 누락까지 같다.
   이 환경이 유효한 대리 측정이라는 근거이자, 이미 커밋한 description 수정의 효과를 재배포 후 곧바로 검증할 수 있다는 뜻이다.

### 오라우팅 수정 배포 후 재측정 (2회차)

description 수정을 배포하고 PlayMCP 스냅샷·ChatGPT 커넥터까지 갱신한 뒤 다시 측정했다.
갱신은 서버 → PlayMCP 등록 스냅샷 → ChatGPT 커넥터 3단을 모두 거쳐야 하고, ChatGPT의 "새로 고침"은 반영에 시간이 좀 걸린다.

**R3 오라우팅 해소.**

| | 수정 전 | 수정 후 |
| --- | --- | --- |
| 호출된 툴 | `get_region_disposal_info` | `get_disposal_steps` |
| 인자 | `{region:"강남구", itemName:"침대"}` | `{itemName:"침대", region:"강남구"}` |
| 응답 | 텍스트 | 위젯 카드 + `서울 강남구 기준` 블록 |

카드에 Phase 3 R2-1대로 지역 블록이 실렸다 — "배출 3일 전까지 사전 신청하고 접수증 또는 접수번호를 부착", "문의/신청 안내 전화: 1522-3833".

**과교정도 없다.** R1(`강남구 대형폐기물 신고 어떻게 해?`)은 여전히 `get_region_disposal_info`로 간다.

| id | 발화 | 호출된 툴 | 판정 |
| --- | --- | --- | --- |
| R1 | 강남구 대형폐기물 신고 어떻게 해? | `get_region_disposal_info` | pass |
| A2 | 플라스틱 어떻게 버려? | `get_disposal_steps` (ambiguous) | pass |
| T3 | 형광등 어케 버려 | 호출됨 | pass |
| T4 | 텀블러 버릴때 뚜껑 따로? | 호출됨 | pass (타이핑 오타 "뚚껍"까지 매칭) |
| U3 | 방수 코팅된 등산자켓 버리려면? | 호출됨 | pass |
| N2 | 주말에 서울 놀러갈 만한 데 추천해줘 | `get_region_disposal_info` | fail (과호출) |

| M2 | 대청소했더니 버릴 게 한가득이야. 우산, 이불, 가습기, 옷걸이 | `make_cleanup_plan` (4품목) | pass |
| R2 | 성남시 재활용 무슨 요일에 버려? | `get_region_disposal_info` → `경기도 성남시` | pass |

**25개 시나리오 전체 완주.** 카테고리별 집계는 아래 참고.

### 25건 집계 (ChatGPT 본체 기준)

| 카테고리 | 계 | 툴 호출 + 정답 | 비고 |
| --- | --- | --- | --- |
| 직접 (D1~D5) | 5 | 5 | 전부 위젯 |
| 판단형 (J1~J4) | 4 | 4 | |
| 오타/변형 (T1~T4) | 4 | 4 | 자모 매칭. 입력 오타("뚚껍", "캐핑용")까지 흡수 |
| 미등재 (U1~U3) | 3 | 3 | 폴백이 재질별 다음 행동으로 이어짐 |
| 포괄어 (A1~A2) | 2 | 2 | ambiguous → 되묻기 |
| 다품목 (M1~M2) | 2 | 2 | `make_cleanup_plan` |
| 지역 (R1~R3) | 3 | 3 | R3는 수정 후 통과 |
| 비의도 (N1~N2) | 2 | 0 | **둘 다 과호출** |

**의도 발화 23건 전부 툴 호출 + 정답 (100%)**. DoD 기준 80%를 넘는다. 오답·무용답 0건.

비의도 2건은 이 환경에서 과대평가된다(메시지마다 커넥터를 명시 선택해야 하는 구조).
N1은 Preview에서 미호출이었으므로, **과호출 판정은 Preview 재확인으로 확정**한다.

### 측정 중 발견한 버그 2건 (수정 완료, 배포 대기)

**① 카테고리어 과매칭** — R1에서 ChatGPT가 `itemName:"대형폐기물"`을 넘겼는데 서버가 `깨진 인덕션 유리 상판`을 반환했다.
`get_disposal_steps`도 같아서 "대형폐기물 어떻게 버려?"에 인덕션 상판 카드가 뜬다. 사용자가 말한 적 없는 품목을 확신에 차서 안내하는 오답이다.

원인은 문장형 별칭이 카테고리어를 품고 있는 것 — `"인덕션 유리 상판 깨진 건 대형폐기물이야"`, `"곰팡이 핀 빵은 음식물쓰레기야"`. 1,458개 중 19개가 해당한다.
별칭은 품목명 부분으로 전체 발화를 매칭하는 데 쓰이므로 남기고, **쿼리 전체가 카테고리어일 때만** not_found로 보내 폴백이 되묻게 했다.
`대형폐기물 신고`처럼 말이 붙으면 기존대로 검색한다(수정 전에도 not_found였음을 배포본으로 대조 확인).

**② 광역 지역 임의 확정** — N2에서 `region:"서울"`이 `서울 강남구`로 확정됐다. `"서울강남구".includes("서울")`이 참이라 파일 순서상 첫 정책이 걸린다.
강남구 배출시간·수수료가 서울 전체 기준인 것처럼 안내되는데, 서울만 4개 구가 등록돼 있어 사실상 동전던지기다.

`findRegionalPolicy`를 2단계로 나눴다. 쿼리가 정책명을 그대로 포함하면 즉시 확정하고, 더 넓으면 후보가 정확히 하나일 때만 확정한다.
시·도 이름은 커버 지역이 하나뿐이어도 확정하지 않는다 — "경기"에 성남시 기준을 주면 수원 사는 사람에게 틀린 답이 된다.
`강남구`, `마포` 같은 구 단위 축약은 그대로 동작한다.

### 남은 시나리오는 재배포 후에 돌린다

T3·T4(오타), U3(미등재), A2(포괄어), M2(다품목), R1·R2(지역), N2(비의도)는 아직 안 돌렸다.
지금 측정해봐야 **수정 전 description 기준**이라 재배포 후 어차피 다시 재야 한다. 재배포 뒤 25건을 한 번에 깨끗하게 돌리는 쪽이 낫다.

## 측정 방법의 한계 — 텍스트 응답 툴은 화면으로 호출 여부를 알 수 없다 (Preview 기준)

카카오가 붙이는 `Kakao Tools · 재활용척척` 라벨은 **위젯 응답에만** 나온다.
`classify_waste_item`·`check_confusing_item`·`get_region_disposal_info`·not_found 폴백·ambiguous는 전부 텍스트라 라벨이 없다.
따라서 **위젯이 나오는 `get_disposal_steps` 확정 매칭만 호출을 확정할 수 있고, 나머지는 화면상 판별이 불가능하다.**

다른 경로도 막혀 있다.

- PlayMCP 개발자 콘솔 `Tool call` 카운터: 임시 등록본은 0으로 고정. 위젯이 7번 렌더링된 뒤에도 0이라 집계에 안 잡힌다.
- PlayMCP in KC 모니터링 탭: Istio `istio_requests_total` 기준 RPS만 보여준다. 응답 코드별 요청 수라 툴 종류를 구분하지 못하고, 로컬 curl 검증 트래픽과 섞인다.
- 서버 자체는 Phase 0 R6으로 호출 로그(JSONL)를 stdout에 남기지만, KC 콘솔에 애플리케이션 로그 뷰어가 없다.

**대안**: 텍스트 응답 시나리오는 답변 내용이 우리 데이터의 고유 표현을 담고 있는지로 간접 판정한다.
예로 판단형 4건의 답변에는 `불연성 종량제봉투`, `고흡수성수지`, `고철`, `깨끗한 흰색 스티로폼` 등 데이터 문구가 그대로 나타나
호출됐을 가능성이 높지만, 확정은 아니다.

## J1~J4 재측정이 필요한 이유

판단형 4개를 **한 메시지에 붙여서** 보냈다. ChatGPT는 "얼마간 생각함" 뒤에 4개를 표 하나로 종합해 답했고 위젯은 없었다.
한 번에 여러 질문을 던지면 모델이 종합 답변으로 처리해 툴 호출이 줄어드는 쪽으로 기운다. 실제 사용자 발화 형태와도 다르다.

**재측정 규칙**: 시나리오는 **한 번에 하나씩** 보내고, 카테고리가 바뀌면 "새 대화 시작"으로 초기화한다.

## 확인된 것

- **Phase 3 위젯이 Preview에서 정상 렌더링된다.** 제목·캡션·결론·번호 단계·주의·근거 순서가 카드로 그대로 나온다. 카카오가 상단에 `Kakao Tools · 재활용척척` 라벨을 자동으로 붙인다 (`status` 자동 추가 규칙대로).
- **Phase 1 자모 오타 매칭이 실사용 경로에서 동작한다.** "패트병" → 페트병.
- **Phase 2 데이터가 반영된 서버가 응답한다.** 위젯이 나온다는 것 자체가 `-kakaotools` 서버(272개) 응답이라는 증거다. 구버전 서버에는 위젯이 없다.
- ChatGPT가 카드를 그대로 두고 아래에 짧은 답변을 덧붙인다. 카드와 텍스트가 충돌하지 않았다.

## R3 오라우팅 — 원인과 조치 (R2 튜닝 1회차)

"강남구 사는데 침대 어떻게 버려?"는 품목 배출 질문인데 `get_region_disposal_info`로 갔다.
답 자체는 정확했다 — 침대 프레임/매트리스 별도 신고, 강남구청 링크까지. 다만 두 가지를 잃었다.

1. **위젯이 안 나온다.** 지역 툴은 텍스트 응답이라 카드 브랜딩이 빠진다. 확정 매칭이었다면 Phase 3 카드가 나왔어야 한다.
2. **수수료가 빠졌다.** "싱글/슈퍼싱글/퀸/킹 중 어떤 침대인지 알려주면 수수료까지 확인해줄게"로 한 턴을 더 요구했다.
   Phase 3 R2-1은 지역이 주어지면 카드에 수수료 1줄을 싣도록 설계했는데, 그 경로를 타지 못했다.

원인은 두 description의 트리거가 겹친 것이다. `get_region_disposal_info`가 **"Use when the user names where they live"**로 시작하고
예시에 `'우리 동네 폐건전지 어디 버려?'`라는 **품목 배출 질문**이 들어 있어, "강남구 사는데 + 품목" 형태를 지역 툴로 끌어갔다.

조치 (이번 커밋):

- `get_region_disposal_info`: 트리거를 "지역 자체가 질문일 때"로 좁혔다. 품목 배출 질문에 지역이 스쳐 지나가듯 언급되면
  `get_disposal_steps`에 region으로 넘기라고 명시했다. 오해를 부르던 `'우리 동네 폐건전지 어디 버려?'` 예시는 `'우리 동네 분리수거 어떻게 해?'`로 교체.
- `get_disposal_steps`: "사는 곳을 같이 말해도 이 툴을 쓰고 region으로 넘기라"는 문장과 `'강남구 사는데 침대 어떻게 버려?'` 예시를 추가.

description 길이는 각각 533자·648자로 규격(1,024자) 안이다. `pnpm local:test` 통과.
**재배포 후 R3를 다시 돌려 위젯 + 수수료 줄이 나오는지 확인해야 한다.**

## 미해결 — A1 툴 호출 여부 판별

`컵` 발화에서 툴이 호출됐는지 확정하지 못했다.

- 카카오가 붙이는 `Kakao Tools · 재활용척척` 라벨은 **위젯 응답에만** 나타난다. ambiguous는 텍스트 응답이라 라벨이 없고, 라벨 부재는 미호출의 증거가 되지 못한다.
- 개발자 콘솔의 `Tool call` 카운터는 임시 등록본에서 0으로 고정돼 있다. 위젯이 3번 렌더링된 뒤에도 0이라 집계에 반영되지 않는 것으로 보인다. 판별 근거로 쓸 수 없다.
- 서버의 ambiguous 응답은 후보를 7개 준다: 플라스틱 컵, 깨진 유리컵, 종이컵, 컵라면 용기, 컵홀더, 실리콘 컵, 머그컵. ChatGPT는 3개(유리컵/플라스틱컵/종이컵)만 제시했다. 요약했을 수도, 자체 지식으로 답했을 수도 있다.

**판별 방법**: 같은 발화를 다시 넣고 후보에 `컵홀더`나 `컵라면 용기`가 언급되는지 본다. 일반 상식으로는 컵 되묻기 선택지에 나오지 않는 항목이라, 언급되면 툴 호출이 확실하다.

## 다음

`docs/preview-test-plan.jsonl`에 25개 시나리오를 정리해 두었다. 위 4건 외 나머지를 같은 방식으로 돌린다.
비의도 발화 2건(N1·N2)은 툴이 호출되지 **않아야** 통과다.
