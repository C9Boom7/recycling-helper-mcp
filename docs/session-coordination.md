# Session Coordination

이 문서는 `재활용척척` 고도화를 여러 Codex 세션에서 병렬로 진행할 때의 작업 분리 기준이다. 이 세션은 조율 세션으로 사용하고, 개별 작업 세션은 아래 소유 범위를 벗어나는 변경을 최소화한다.

## Active Threads

| Track | Codex thread | Current role | Primary files |
| --- | --- | --- | --- |
| Source Gap | `019f06b4-641c-7b92-b668-1ad70c6abed3` | 공식 근거 탐색, `needs_source` 해소, 출처 커버리지 문서 갱신 | `src/data/waste-items.json`, `docs/source-*`, `docs/source-coverage.md` |
| Region Data | `019f2292-2522-70c0-8e74-48aa4f37c968` | 지역 정책 데이터 확장, 지역별 답변 회귀 검증 | `src/data/region-policies.json`, `src/data/region-evaluation-cases.json`, `docs/*region*`, `src/server.ts`의 지역 문구 |
| Answer Quality | `019f2291-ccd0-70f0-95d0-fbe8d5d17ee8` | 질문 seed, 자동 백로그, MCP 답변 smoke 회귀셋, 매칭 방어 로직 | `logs/quality-seed-queries.example.jsonl`, `src/data/mcp-answer-cases.json`, `src/data/question-backlog.json`, `src/data.ts`, `scripts/auto-backlog-queries.mjs` |

## Separation Rules

- 한 세션은 자기 트랙의 primary files만 주도적으로 수정한다.
- 공유 파일은 변경 전후로 조율 세션에서 확인한다.
- `src/data/mcp-answer-cases.json`, `src/data/question-backlog.json`, `docs/question-backlog.md`는 여러 트랙이 만질 수 있으므로, 변경 이유와 카운트를 최종 답변에 반드시 남긴다.
- 크리티컬한 사용자 결정만 즉시 묻는다. 크리티컬 기준은 원격 push/PR/배포, 큰 리팩터링, 위험한 삭제, 보안/개인정보/운영 영향, 제품 범위·데이터 모델의 큰 변경, 또는 합리적인 기본값 없이 다음 작업이 막히는 경우다.
- 시급도가 낮거나 진행을 막지 않는 데이터셋 모델링 결정은 즉시 사용자에게 묻지 않고 `docs/data-decision-backlog.md`의 `Open Decisions`에 출처/후보/추천안과 함께 쌓는다. 열린 결정이 10건 이상이면 조율 세션에서 사용자에게 묶어서 확인한다.
- `src/server.ts`, `src/data.ts`, `scripts/*.mjs`는 동작 변경이므로 다른 세션의 데이터 작업 중에는 최소 범위만 수정한다.
- 지역 데이터가 늘어난 뒤에는 unknown-region 기준 smoke 케이스가 실제 지역 기준과 충돌할 수 있으므로 Answer Quality 트랙에서 기대값을 재확인한다.
- 품목 데이터가 늘어난 뒤에는 Source Gap 트랙이 `source-coverage.md` 카운트, Answer Quality 트랙이 매칭/과매칭 회귀를 확인한다.
- 같은 체크아웃에서 작업하므로 세션 간 변경을 되돌리지 않는다. 충돌처럼 보이면 먼저 조율 세션에서 `git status`, 관련 diff, thread 상태를 확인한다.

## Coordination Loop

- 세 작업 세션을 전체 배치로 묶어 기다리지 않는다.
- 각 heartbeat마다 세션별 status를 확인하고, `active`인 세션은 그대로 둔다.
- `idle`인 세션은 해당 세션의 최신 final answer, 변경 파일, 검증 결과, 남은 리스크를 확인한 뒤 그 트랙의 다음 작업만 배정한다.
- 새 작업은 현재 `active` 세션의 primary files와 겹치지 않게 잡는다.
- 다음 자연스러운 작업이 없거나 사용자 판단이 필요한 비데이터 결정이면 배정하지 않고 조율 세션에 보고한다.
- 데이터셋 모델링 결정은 `docs/data-decision-backlog.md`에 누적하고, 열린 결정이 10건 이상일 때만 사용자에게 묶어서 확인한다.

## Handoff Checklist

각 작업 세션이 idle로 돌아오면 조율 세션은 해당 세션 단위로 아래 순서에 맞춰 상태를 확인한다.

1. `git status --short`로 변경 파일을 확인한다.
2. 변경 트랙과 무관한 파일이 섞였는지 확인한다.
3. 카운트 문서가 실제 데이터와 맞는지 확인한다.
4. 최소 검증을 실행한다.
   - 데이터/서버 변경: `pnpm check`
   - MCP 답변 케이스 변경: `pnpm smoke:mcp`
   - 백로그 변경: `pnpm backlog:questions` 또는 `pnpm backlog:auto:quality`
5. 다음 병렬 작업은 primary files가 겹치지 않도록 다시 2~3개로만 쪼갠다.

## Current State Snapshot

- Source Gap 트랙: 현재 데이터 기준 `waste-items.json` 336개, `evaluation-cases.json` 336개, MCP answer cases 632개, review count는 `verified 44 / region_review_needed 91 / needs_source 7`이다. 표준 티어 `standard_import` 194개(Phase 2의 142개 + Phase 7의 52개)가 여기에 더해진다. 남은 `needs_source` 7개는 공식 단독 근거가 없어 `source-gap-policy.md` 기준의 보수 안내로 유지한다.
- Source Gap 조사: Top 50/사용자 빈도 출처 감사에서 초기 10개, `home_injection_needle`, `empty_medicine_bottle`, `pill_blister_packaging`, `small_electronics` 보강분, `shoes`, `cat_litter`, `delivery_box`, `bubble_wrap`, `mirror`, `clothing`, `paper_cup`, `shipping_label`, `receipt`, `shampoo_bottle`, `pizza_box_oily`, `medicine`, `spray_can` source/basis 보강과 문서 QA가 완료됐다. 2026-07-05 사용자 결정분으로 `비닐약봉지`는 `snack_bag` 별칭/source로 흡수했고, `비닐장판`(`vinyl_flooring`), `캔버스 액자`(`canvas_frame`), 스탠드형 행거(`standing_clothes_hanger`)는 별도 품목과 평가 케이스로 추가했다. `cup_noodle_container`는 `종이 컵라면` 공식 근거를 조건 보강으로 반영했다.
- Region Data 트랙: 현재 지역 정책 데이터 57개, 지역 평가 케이스 136개다. Phase 5에서 티어를 나눴다 — `full` 5개(`gangnam_gu`, `seocho_gu`, `songpa_gu`, `mapo_gu`, `seongnam_si`)는 배출 요일까지, `standard` 35개는 자치구·시의 대형폐기물 신청·수수료 URL과 직통번호, 폐의약품·폐건전지 수거함 안내까지, `metro` 17개는 광역시도 폴백만 담는다. 강남 음식물 예외 4개(`fruit_seed`, `fish_bone`, `tea_bag`, `onion_peel`)는 공식 첨부 PDF 기반 전용 itemGuide와 회귀 케이스로 고정했고, 마포구와 성남시/판교는 공식 출처 기반 생활쓰레기/재활용/음식물/폐형광등·폐건전지/대형폐기물 기본 안내를 갖고 있다. 2026-08-16에 남은 서울 8개 구(중구·성동구·동대문구·중랑구·성북구·서대문구·양천구·구로구)를 넣어 **서울 25개 구가 전부 등록됐다**. 2026-08-16에 `metro` 17곳의 별칭에 소속 기초자치단체 이름 168개를 넣어, 등록되지 않은 시·군·구도 이름만 대면 광역 안내로 착지한다 — 그전에는 `수원시`가 광역 폴백조차 못 받고 전국 폴백으로 떨어졌다. 전국 동명(`중구`·`동구`·`서구`·`남구`·`북구`·`강서구`·`고성군`)과 자체 정책 보유 지역은 넣지 않았고, 광역 착지에는 분리배출.kr·정부24 링크를 함께 남겨 안내가 얕아지지 않게 했다. 2026-08-17에 그 이름들을 `aliases`에서 새 필드 `districtAliases`로 갈랐다 — 광역 자신을 부르는 표기와 광역이 대신 받는 시·군·구 이름은 뜻이 달라서, 한 배열에 섞여 있는 동안에는 `청주시`로 착지한 응답이 "거주 중인 시·군·구를 알려주세요"라고 되물었다. 같은 날 동명 지역 28개를 또 다른 필드 `prefixOnlyDistrictAliases`로 넣었다(광역시 6곳의 중구·동구·서구·남구·북구·강서구, 경기 광주시, 강원·경남 고성군) — 매칭에는 쓰지 않아 맨 `중구`는 그대로 되묻지만, 앞에 광역이 붙어 이미 확정된 `부산 중구`는 응답이 그 이름을 부른다. 보류 사유 여덟 개 중 여섯 개는 리서치 방법을 바꾸니 그대로 풀렸다 — 서대문 폐의약품과 구로 폐의약품은 본문이 이미지라 서버 응답이 비어 보였을 뿐이고(이미지를 읽어 확인), 성북 폐건전지는 메뉴가 아니라 청소/환경 게시판 글에, 양천 전지·형광등은 배출요령 PDF에, 구로 폐건전지는 수거함 정비 보도자료에 있었다. 중랑 직통번호는 스마트클린 중랑(중랑구청이 운영하는 대형폐기물 시스템)에 걸린 02-2094-1956으로 채웠다 — 구청 대표번호 02-2094-0114와는 다른, 대형폐기물 단일 창구다. 성동 인터넷 신고는 구청 자체 신청 경로로 채웠다(신청 화면이 로그인 뒤라 `applicationUrl`은 신청하기 버튼이 있는 안내 페이지 `sd.go.kr/main/contents.do?key=1670`을 가리킨다). 중구·동대문구는 신청을 상용 앱('빼기'·'여기로')에 위임해 구 자체 신청 폼이 없어, 사용자 결정에 따라 구청 대형폐기물 안내 페이지를 `applicationUrl`로 쓰고 수수료는 구 폐기물 관리 조례 별표를 건다. Phase 9에서 서울·경기 밖 광역시 자치구를 열었다 — 2026-08-22에 해운대·부산진·대구 북구·남동·부평 5곳, 2026-08-23에 대구 달서구와 광주 북구 2곳, 2026-08-25에 대전 서구까지 8곳 전부 들어갔다. **대전 서구는 2026-08-25에 마지막으로 들어갔다** — 구청 누리집에 폐의약품 안내가 없어 한동안 막혀 있었는데, 출처가 두 곳으로 갈려 있었다. 수거 장소는 **구 자체 데이터포털의 오픈API**(`ds_pk=55`)가 297곳을 서비스하고 있었고(공공데이터포털 쪽 데이터셋은 내려가 404였다), 약 형태별 배출 방법은 대전시 자원순환과 안내 게시글의 이미지 세 장에 있었다. 구청 메뉴만 훑어서는 둘 다 안 보인다. 동명 자치구(`북구`·`서구`)는 맨 이름을 `aliases`에 넣지 않고 광역 접두어가 붙은 표기만 쓴다. 광주는 2026년에 **전남광주통합특별시**로 이름이 바뀌어 구청·시청 누리집이 모두 새 이름을 쓴다. 전라남도 누리집도 같은 이름을 쓴다 — 통합된 두 광역이 이름 하나를 나눠 쓰는 셈이다. 처음엔 그 표기를 광주 별칭으로 넣었다가 뺐다 — `전남`이 전라남도 별칭이라 `전남광주 광산구`가 두 광역에 동시에 걸렸다(naming sweep 8건 실패). 2026-08-23에 "질의 앞부분을 더 많이 먹은 표기가 이긴다"는 규칙으로 되돌렸는데, 그 규칙이 이번엔 **전남 22개 시·군을 통째로 광주에 넘겼다** — `전남광주통합특별시 목포시`에서 `전남광주통합특별시`(9자)가 `전남`(2자)을 이겨, 목포 주민이 광주 안내를 받는다. 2026-08-24에 길이 대신 **뒤에 붙은 이름의 주인**으로 가르도록 다시 고쳤다(`remainderOwner`). 강도 2(질의가 지역 표기로 시작)에서 후보가 여럿이면 앞의 표기를 뗀 나머지를 각 광역이 하위 지역으로 알아보는지 보고, 알아보는 쪽이 하나면 그쪽이 이긴다 — `목포시`가 붙으면 전라남도고 `광산구`가 붙으면 광주다. 아무도 못 알아보거나 둘 다 알아보면 되묻는다(`전남광주통합특별시 어쩌구`). 나눠 쓰는 표기는 전라남도 쪽에 새 필드 `sharedAliases`로 적었다 — 완전 일치에서는 빠져서 `전남광주통합특별시` 단독 질의는 그대로 광주로 가고, 뒤에 시·군 이름이 붙어 강도 2로 걸릴 때만 후보로 선다. `findNamedSubRegion`도 이 표기를 떼기 때문에 응답이 `목포시`를 그대로 부른다. 규칙을 넣기 전에는 `전남광주통합특별시 북구`가 **전라남도로 착지하고 있었다** — 확정 실패가 아니라 오라우팅이라 개칭 전에도 있던 구멍인데 새 이름이 생기며 실제 발화가 됐다. 잘린 광역 이름이 자치구로 새지 않게 `광주광`·`광주광역`도 광역 별칭에 뒀다. 광주 회귀가 `pnpm check`를 그대로 통과한 건 naming sweep이 광역을 **자기** 별칭과만 교차했기 때문이라, 한 광역의 표기가 다른 광역 표기의 앞부분일 때 교차 조합을 만드는 스윕을 더했다(지금은 전남·광주 한 쌍에서 52건). 전 조합은 돌리지 않는다 — `부산 목포시` 같은 짝은 부산으로 가는 게 맞아 기대값을 적을 수가 없다. 이 스윕은 `sharedAliases`까지 훑어야 대상이 잡힌다 — 통합 표기를 그 필드로 옮긴 뒤 한동안 **0쌍**을 돌고 있었고, 지금은 0쌍이면 실패로 끊는다. 2026-08-25에 **출처 자리 경쟁을 없앴다.** 구조화 응답은 출처를 앞 세 개만 싣는데(`pickRegionOfficialSources`, 상한 3) 12곳에서 폐의약품·폐건전지 안내가 그 밖으로 밀려 있었다. 처음엔 순서만 바꿨다가 리뷰에서 되짚었다 — 그 자리를 차지하던 것이 `bulkyWaste`의 신청·수수료 주소였고, **구조화 페이로드에는 그 주소를 담는 자리가 거기밖에 없어서** 순서만 바꾸면 8곳에서 신청 주소가 통째로 사라진다. 본문에 있다는 근거는 텍스트를 읽는 호스트에만 통하고, 이 변경이 겨냥한 쪽에는 안 통했다. 그래서 `bulkyWasteContact`를 제 필드로 냈다(전화·신청 URL·수수료 URL). 구조화가 지역당 170~185B 늘고 텍스트는 그대로다. 이제 안내 근거와 신청 주소가 자리를 다투지 않는다. **다만 공짜는 아니다** — 창이 둘뿐이라 안내 둘을 앉히면 10곳에서 대형폐기물 설명 페이지가 구조화 밖으로 나간다(신청·수수료 주소는 `bulkyWasteContact`가 나르므로 사라지는 건 절차 설명 쪽이다). 새로 실리는 출처 33개와 맞바꾼 값이다. 스윕을 돌리다 **본문은 수거함 안내를 말하는데 근거 출처가 아예 없는 자리 15곳**도 드러났다 — `KNOWN_GUIDE_SOURCE_GAPS`에 이름을 남기고 `data-decision-backlog.md`에 올렸다. 순서는 규칙으로 세웠다 — **종류마다(의약품·전지) 첫 출처를 먼저 앉히고**, 같은 종류의 둘째, 그 밖의 출처, 본문·`bulkyWasteContact`가 이미 나르는 주소 순이다. 자치구 40곳에 걸어 38곳의 순서가 바뀌었고, 출처를 넣거나 뺀 곳은 0이며 `findRegionCollectionDaySource`가 고르는 페이지도 그대로다. 함정이 둘 있었다. **유효 창은 3이 아니라 2다** — 요일 출처가 셋째 자리를 예약하므로 `sources.slice(0, 3)`로 재면 틀린다. 그리고 **안내인지는 제목이 아니라 `basis`까지 봐야 한다** — 서대문 「폐금속자원 배출」과 수원 「재활용분리배출」은 제목에 낱말이 없고 근거에만 있다. 제목만 보고 재다가 "0곳"이라고 잘못 보고한 적이 있다. 2026-08-26에 맨 동명 이름의 되묻기 분기를 넣었다 — `prefixOnlyDistrictAliases`는 매칭 표에서 빠져 있어 맨 "중구"가 후보 0개로 not_found까지 떨어졌고, 서울 중구 데이터를 갖고 있으면서 "상세 지역 데이터가 없습니다"라고 답했다. 질의가 이 목록과 정확히 일치하면 같은 표기의 정식 별칭 보유 지역(광주광역시), 마지막 어절이 같은 등록 자치구(서울 중구·강서구, 대구·광주 북구, 대전 서구), prefixOnly 보유 광역을 전부 후보로 세워 되묻는다. "광주시"도 광주광역시 단독 확정에서 광주·경기 되묻기로 바뀌었다 — 별칭에서 빼는 해법은 기각했다. 빼면 "광주시 서구" 경로의 지목이 깨지고 되묻기 후보에서 광주광역시가 빠진다. prefixOnly 스윕에 양성 단언(ambiguous + 해당 광역 포함)을 더했고 지역 평가 케이스 5건, answer 케이스 재작성 2건·신규 1건으로 고정했다.
- Region Fees: 수수료 보유 지역은 28곳, 총 3,829행이다(2026-08-23 세 트랙 전체 재임포트 — 상한에 걸린 (지역, 품목) 7쌍에 `preCapFeeRowCountByItemId`를 채워 "확인된 N개 규격 중 대표 12개" 줄이 실제로 나가고, 8/19 규칙 정리(돗자리·인형·장난감)가 강남 밖 지역에도 적용돼 22행이 늘었으며, 표준데이터 임포터에 대형폐기물 갈래 문턱을 붙여 런타임이 읽지 않던 행 96개를 뺐다. 금액 변경 0행. 자세한 건 [post-finals-backlog.md](post-finals-backlog.md) 2번). **서울 25개 구는 전부 들어왔다.** 2026-08-16 조례 ETL로 신규 8개 구를 채우려다 실패해, 트랙을 하나 더 만들었다 — `fees:fetch:district` → `import:district`가 **구청이 직접 띄운 수수료표**를 읽어 성북 228·중랑 176·양천 187·서대문 101·성동 109·구로 92행을 넣었다(893행). 조례 별표는 HWP 표를 한 줄로 펴야 해서 품명이 병합 셀에서 이어지고, 그 이어짐이 어긋나면 이웃 품목의 규격·금액이 넘어온다 — 서대문 「세탁기 / 소형(60X100 미만) / 1,000원」은 고무통 값이었다. 구청 표는 품목·규격·금액이 이미 갈려 있어 그 갈래가 없고, 조례가 아예 못 뽑던 성동·양천·구로도 열린다. **893행을 원문과 전수 대조해 불일치 0건**을 확인하고 넣었다. 중구·동대문구는 구청이 기계로 읽을 표를 안 내놔(중구는 조례만, 동대문은 HWP 첨부) 조례 파서를 고쳐야 열린다. 골든셋은 셋으로 줄었다 — 강남 19, 서초 18, 송파 18행은 수기 데이터를 유지하지만 **마포는 2026-08-19에 조례 추출(140행)로 갈아탔다.** **2026-08-26에 이 구조를 바꿨다** — 수기 36행(서초·송파)을 `scripts/fixtures/ordinance-golden-rows.json`으로 동결하고, 두 지역의 운영 데이터는 전량 수집으로 갈았다(서초 조례 122행, 송파 구청 표 156행). 그전에는 골든셋이 곧 운영 데이터라 커버리지를 넓히는 순간 검증이 죽었다 — 검증이 확장을 막고 있던 셈이다. 수기 14행이 Top 50 범위뿐이라 침대 프레임조차 없었기 때문이다. 갈아탄 지역은 R2 대조 기준에서 뺀다 — 우리 행이 검증이 다시 돌리는 그 파서에서 나오면 하한선이 자기 충족이 된다. 잃은 것도 있다: `좌식의자, 접의자` 행이 `multi_item_name`으로 빠지고, 요가매트는 돗자리가 `picnic_mat`으로 제자리를 찾으며 금액이 없어졌다(그 자리를 메우던 「유아용 놀이매트」는 요가매트가 아니라 `HEAD_COLLISION_NAMES`로 막았다).  Phase 7이 표준데이터에서 용산 229·노원 263·강서 147·관악 141행을 임포트했다(`pnpm import:fees`로 재현). 신규 품목 6개(화장대·간판·복사기·가스레인지·옥매트·세단기)를 추가한 뒤 같은 4곳만 다시 임포트해 26행이 늘었다. Phase 6은 자치법규 조례에서 나머지 10곳 1,176행을 뽑았다(성남 77·종로 68·광진 156·강북 106·도봉 146·은평 160·금천 110·영등포 149·동작 94·강동 110, `pnpm fees:fetch` 후 `pnpm import:ordinance`로 재현). 조례 트랙은 `sourceType: "law"`로 조례 URL·별표 제목·시행일자를 근거에 함께 싣고, 표준데이터 규칙에 더해 무상수거 행 제외, 품명 상속 행 판정, 짧은 별칭 수식어 위치 검사, 매트리스와 침대 프레임 분리를 건다. 코드 리뷰 후속으로 갈래 판정을 더 조였다 — `매트리스제외`·`매트리스포함`은 매트리스로 보내지 않고, 돌·옥·황토 침대는 `stone_bed`로 가르며, 규격 칸이 다른 대형폐기물 품목으로 확정되는 행과 라벨은 같은데 금액만 다른 행은 넣지 않는다. 표준데이터 임포트는 오타 매칭·복수 품목 표기·수식어 위치 매칭·옵션 요금 행을 배제하는 보수 기준을 쓴다. 마포구는 조례 임포트로 73개 itemId까지 넓어졌다(수기 시절 7개). 2026-08-23에 서울 밖 광역시 자치구 둘(대구 달서구·광주 북구)이 **구청 표 트랙**으로 붙었다. 표준데이터를 안 쓴 이유가 지역마다 다르다 — 광주 북구는 아예 미수록이고, 달서구는 209행이 있지만 규격 칸이 전부 비어 있어 냉장고가 5,000~20,000원 다섯 행인데 어느 크기인지 알 수 없다. 구청 표에는 「700ℓ이상 15,000」처럼 규격이 붙는다. 2026-08-24 리뷰 후속으로 표기 별칭 넷(`응접세트`·`응접셋트`·`텔레비젼`·`장농`·`캐비넷`)과 자간 벌리기 정리가 조례 트랙에도 닿아 7곳이 함께 움직였다 — 성남·은평·동작·강동은 그동안 `not_found`로 빠지던 행을 되찾았고(합 17행), 종로·광진·마포는 「문 짝」·「밥 상」·「문 갑」 같은 라벨이 붙었다.
- Answer Quality 트랙: seed 116개, MCP answer cases 632개, Backlog는 `covered 110 / wont_fix 1 / todo 0`이다. Phase 1(2026-08-14)에서 자모 오타 매칭, not_found 재질 폴백, 별칭 보강(멜라민 스펀지·우유곽·밧데리·페트) smoke 15건을 추가했다. Phase 2(2026-08-14)에서 표준 티어 142개와 지역 확인 필수 품목 answer case 75건을 추가했고, 정수기 필터(auto_b6aece0913) todo는 `water_purifier_filter` 품목으로 커버됐다. Phase 1 코드 리뷰 후속(2026-08-14)으로 오타 매칭을 폴백 티어로 분리하고 첫 자모 일치를 요구하도록 좁혀 smoke 7건을 추가했다. 최근 source/region 보강 축과 `needs_source` 7개 보수 안내, `medicine`/폐의약품 포장재 분리, `spray_can` 찌그러짐 조건, 2026-07-05 source reflection smoke, 오피스텔 `get_disposal_steps` guard, Open Decision guard, 마포구 수수료 answer smoke, 지역 영향 분류/cleanup plan 라벨 smoke, cleanup plan 참고-only 과노출 guard, `빈 약통`/`토너 카트리지`/`석고방향제`/`비닐류 포장재`/`시든 꽃` 지역 참고 smoke, `check_confusing_item` 지역 영향 라벨 smoke, 조건형 비닐류/젖은 종이상자/스프레이캔 판단 범위 smoke, `get_region_disposal_info` 판단 범위 smoke, 포괄어 `컵`/`통`/`병`/`용기` 모호 응답 smoke까지 통과했다. Phase 4b 2라운드(2026-08-14)에서 카테고리어 과매칭·광역 지역 임의 확정 회귀 smoke 7건을 추가했다 — 카테고리어는 `findWasteItems` 단계에서 막아 `check_confusing_item`까지 함께 덮고, 시·도 이름은 확정하지 않되 `강남구` 축약은 그대로 확정되는지 함께 고정한다. Phase 5(2026-08-14)에서 지역 매칭을 티어·단계형으로 다시 짜면서 시·도 발화의 기대 동작을 "매칭 없음"에서 "광역 레이어 착지"로 옮겼다 — 한 자치구의 배출 요일·전화번호가 시·도 답변에 실리지 않는지는 그대로 회귀로 막고, 표준 티어 지역·광역 폴백·미등록 폴백 smoke 4건을 더했다. Phase 7 R1(2026-08-14)에서 복합명사의 수식어 자리에 걸리던 `generic_fragment` 오매칭을 막고 smoke 18건을 추가했다 — 수식어 배제 6건(`에어컨`→리모컨 등), 동의어·핵심어 반례 9건(`쇼파`→소파, `장판`→비닐장판 등), 역방향 3건(`리모컨`·`정수기 필터`·`온수매트` 자체 질의는 그대로 확정). 코드 리뷰 후속(2026-08-15)으로 되묻기 유지 2건을 더했다 — 수식어 후보를 후보 목록에서 통째로 지우면 남은 하나가 단독 확정돼 `컴퓨터`가 노트북으로, `유리`가 깨진 보온병으로 바뀌었다. 수식어 후보는 확정 후보에서만 빼고 "질의가 덜 특정됐다"는 신호로는 남긴다. PR #16 코드 리뷰 후속(2026-08-15)으로 신규 품목 데이터 결함 다섯 가지를 고치고 smoke 11건을 더했다 — 조사 자리표시자 `은(는)`/`을(를)` 70곳, 배출 경로어를 품목 별칭으로 쓴 `분리수거함`·`방문`(각각 재활용 수거함 질의와 무상방문수거 질의를 품목 카드로 확정시켰다), 무상방문수거 8품목의 `needsRegionCheck: false`(대형폐기물 폴백을 안내하면서 정작 지역 신청 경로를 통째로 감췄다), 욕조 근거에서 문짝·싱크대·물탱크로 번진 "세라믹·대리석" 주의, 짧은 품목명 `난로`에 닿지 못하던 `전기난로`. R4 임포트 리뷰(2026-08-15)에서는 수수료 쪽 결함 세 가지를 고치고 smoke 5건을 더했다 — 배출 경로가 무상인 품목에까지 금액이 붙던 것을 `findBulkyWasteFees`의 게이트로 막고(스티로폼 재활용 카드에 "수수료 1,000원"이 실렸다), 용산·관악의 매트리스 고시 행이 침대 프레임에 붙던 것을 임포터의 품목 재지정으로 돌려놓았으며, "수수료"라는 글자만 보던 기존 fee 케이스에 실제 금액을 박았다. (지역, 품목) 12행 상한도 임포터에만 있던 것을 validate로 옮겼다. Phase 7 R4(2026-08-15)에서 표준데이터 수수료를 임포트하며 4개 지역 수수료 노출 회귀 4건을 더했다. Phase 7 후속 배치(2026-08-15)에서는 품목이 없어 엉뚱한 후보로 되묻던 여섯 건을 품목으로 채우고 smoke 15건을 더했다 — 신규 품목 확정·수수료 노출 6건, 기존 품목을 뺏기지 않는 반례 7건(`화장대 거울`→거울, `복합기`→프린터, `가스오븐레인지`·`가스레인지 오븐`→가스오븐레인지, `전기매트`→전기장판, `옥침대`→돌침대, `세면대`→세면대), 포괄어 되묻기 유지 `상` 1건, `계란판` 역방향 1건. PR #22 코드 리뷰 후속(2026-08-15)으로 반례 3건을 더했다 — 반례를 새 품목명이 뒤에 오는 복합어로만 짜서 앞에 오는 쪽(`query_contains_name` 96~98점)을 놓쳤다. `복사기 토너 카트리지`가 토너 카트리지(종량제봉투·업체 회수) 대신 복사기 무상방문수거 안내를 내보내고 `화장대 의자`가 의자 대신 화장대 수수료를 붙이던 것을 별칭으로 되돌렸다. 대응 품목이 없어 별칭으로 못 막는 `가스레인지대`는 `data-decision-backlog.md` Open Decisions에 남겼다. 재질 포괄어 라우팅(2026-08-15)에서는 재질만 가리키는 낱말 12개의 **확정만** 막고 smoke 10건을 더했다 — `종이`가 별칭 "코팅 종이"를 통해 코팅지로 확정되던 것을 재질 폴백으로 돌리고, 재질어를 품은 실제 품목(종이컵·종이호일·코팅지·비닐랩)은 그대로 확정되는지 반례로 고정했다. `플라스틱`·`유리`는 되묻기가 더 나은 답이라 일부러 넣지 않았고, 그 동작은 Phase 7 케이스가 이미 고정하고 있다. 게이트는 `findWasteItems`가 아니라 `resolveWasteItem`에 뒀다 — `check_confusing_item`은 전자를 직접 부르는데, "종이"에 코팅지·종이호일을 늘어놓는 건 그 툴에서는 맞는 동작이다. 곁들여 `make_cleanup_plan`·`get_region_disposal_info`의 not_found가 아무 안내 없이 끝나던 것도 재질 원칙 한 줄을 붙이도록 고쳤다. Phase 6 R4(2026-08-15)에서 조례 수수료 10곳을 넣으며 3건을 더했다 — 신규 수수료 지역에서 금액 줄이 실리는지(종로구 소파), 수수료표는 있어도 그 품목 행이 없으면 금액 줄 없이 신청 경로만 나오는지(동작구 캔버스 액자), 광역 폴백에 자치구 금액이 새지 않는지(서울 소파). 둘째 케이스는 처음에 동작구 의자로 잡았다가 캔버스 액자로 옮겼다 — 동작구에 의자 행이 없는 것은 조례에 없어서가 아니라 임포터가 걷어낸 결과라, 추출을 손보면 케이스가 깨진다. 어느 조례 수수료표에도 오르지 않는 품목으로 바꿔 런타임 경로만 남겼다. 수거함 안내 확장(2026-08-16)에서 4건을 더했다 — `regionSpecialCollectionLines`가 `method[0]`만 싣던 것을 **물어본 품목이 걸린 쪽만 전부 펼치도록** 바꿨다. 형광등을 물으면 성북 "평판형 LED는 특수규격봉투"까지, 폐의약품을 물으면 회수봉투 안내까지 나가고, 품목을 안 물으면 예전처럼 두 줄만 나간다(지역 개요 응답 크기 동일). 케이스는 확장·비확장·교차 미노출을 모두 고정한다. 가스레인지 접두어 게이트(2026-08-17)에서 7건을 더했다 — 이름이 접두어로 걸려 `가스레인지 후드`·`가스레인지대`(받침장)까지 "가스 밸브를 잠그고 연결 호스를 분리" 절차로 확정하던 것을 `resolveWasteItem`에서 not_found로 돌렸다. 처음에는 정확히 일치하는 낱말 네 개만 막았는데, PR #42 코드 리뷰에서 `가스렌지 후드`(`가스렌지`는 gas_range의 실제 별칭이고 구어에서 더 흔하다)·`가스레인지 받침`·`빌트인 가스레인지 후드`·`가스레인지 후드 필터`가 그대로 98점 확정으로 새는 것이 드러났다. 표기를 `렌지`→`레인지`로 모은 뒤 포함 규칙으로 바꿨고, `상판`은 가스레인지의 일부라 일부러 뺐다. `가스레인지`·`가스렌지`·`전자레인지`가 그대로 확정되는지는 반례로 고정했다. 게이트가 정확 일치가 아니게 되면서 `reservedQueryWords` 목록도 `isReservedQuery` 함수로 바꿨다 — 목록을 따로 두면 규칙이 늘 때 evaluate의 예약어 검사가 조용히 뒤처지는데, 그 어긋남이 정확히 그 검사가 막으려는 사고다. 광역 착지 문구 분기(2026-08-17)에서 3건을 더했다 — 시·군·구를 지목한 질의(`청주시`)와 광역만 댄 질의(`충북`)가 갈리는지를 양쪽 다 고정하고, 품목을 함께 물었을 때도 되묻기가 사라지는지 본다. `안산시`·`부산 해운대구` 기존 케이스는 새 문구와 `regionStatus: "unregistered_district"`로 갱신했다. 리뷰 후속(2026-08-17)으로 동명 구 3건을 더했다 — `부산 중구`·`대구 동구`처럼 광역이 앞에 붙은 질의는 이름을 부르고, 맨 `중구`는 그대로 전국 폴백에 머무는지를 같은 자리에서 갈라 고정한다. 2라운드(2026-08-17)에서 이름 부르기를 품목 툴로 넓히며 2건을 더했다 — 자기 구를 대는 건 지역 질문보다 품목 질문 쪽이 더 흔한데("청주시 사는데 소파 어떻게 버려?") `get_disposal_steps`·`classify_waste_item`은 여전히 되묻고 있었다. 되묻기가 이름 부르기로 **바뀌기만** 하도록 해 카드의 지역 줄 두 개 예산은 그대로 두고, `서울 종로구` 케이스에 문의 전화·인터넷 신청·수수료 조회 URL을 박아 district 티어의 연락처가 이 갈래로 사라지지 않는지 함께 고정했다. Phase 8 품목 커버리지(2026-08-21)에서 신규 품목 5개와 `rice_sack` 별칭을 넣으며 14건을 더하고 기존 폴백 케이스 3건의 기대값을 확정 매칭으로 바꿨다. PR #61 리뷰 1라운드 후속으로 6건을 더 더했다 — 두 글자 이름 `젖병`에 걸려 젖병 카드를 받던 전기제품을 `bottle_sterilizer`로 넘기고(`젖병 소독기`·`젖병 워머`), 맨 `젖꼭지`를 82점으로 가로채던 `젖병 젖꼭지` 별칭을 빼면서 `젖병`·`젖병 젖꼭지`가 그대로 젖병 카드에 남는지를 반례로 고정했다. 나머지 둘은 폴백 `askFor` 구조화 페이로드를 보는 케이스다 — 기대값을 바꾼 셋이 그 페이로드를 박고 있던 유일한 케이스라, 이 PR로 검증이 0건이 될 뻔했다. 여전히 not_found인 `안전벨트`·`젖꼭지`로 자리를 다시 채웠다. 부품 복합어 게이트(2026-08-22)에서 15건을 더했다 — 품목명 뒤에 부품·부속을 가리키는 낱말이 붙으면 다른 물건인데, 질의가 이름을 품는 방향에는 머리 자리 판정이 없어 `모니터 받침대`가 96점으로 모니터(대형가전 무상방문수거)를, `소파 커버`가 79점으로 소파(대형폐기물 수수료)를 확정하고 있었다. `가스레인지 후드`·`화장대 거울`·`복사기 토너 카트리지`를 하나씩 손으로 막아 온 게 전부 이 빠진 절반이다. 부품어를 `compound-part-nouns.json`으로 빼고, 그 품목의 이름이 나온 자리가 전부 부품어를 물고 있으면 `part_compound_fragment`로 강등한다. 등장 자리를 어절 첫머리로 제한하는 게 중요했다 — `침대 프레임 받침대`의 `받침대`에 `침대`가 박혀 있어서, 그 자리를 인정하면 뒤에 아무것도 없다는 이유로 게이트가 풀렸다. 강등된 히트를 `modifier_fragment`와 같은 종류로 두면 `findWasteItems`가 오타 폴백으로 내려가 자모가 비슷한 엉뚱한 품목을 짚는데(첫 측정에서 47건), 종류를 갈라 막았다. 1글자 부품어는 용언 활용형과 겹쳐(`소파 살 거예요`, `이불 줄 거예요`) 넣지 않았고 validate가 2글자 미만을 막는다. 케이스는 부품 복합어 not_found 6건, 꼬리 품목 확정 3건, 게이트가 삼키면 안 되는 canary 6건이다. PR #63 리뷰 후속(2026-08-22)으로 네 가지를 고치고 6건을 더했다 — (1) 자리 판정이 `Math.max`로 오른쪽 끝 한 자리만 보고 있어 `냉장고 버리는데 냉장고 야채칸은 어떻게 하나요`가 not_found로 떨어졌다. 짧은 별칭 누수(`에어컨 실외기 커버`)는 자리 자체를 늘려서 막고 — 같은 자리에서 시작하는 더 긴 이름과, 다음 어절이 같은 품목의 또 다른 이름이면 거기까지 — 깨끗한 자리가 하나라도 있으면 발동하지 않는 전(全)자리 규칙으로 되돌렸다. (2) `뚜껑`을 목록에서 뺐다. 본체 카드 본문(요약·절차·주의)이 뚜껑을 언급하는 34개 품목이 전부 처리까지 적고 있어 `페트병 뚜껑`·`우유팩 뚜껑`·`볼펜 뚜껑`의 맞던 답만 지우고 있었다. 나머지 31개도 같은 기준(본체 카드가 그 부품의 배출 경로를 짚는가)으로 전수 확인했고, 애매한 여섯 개는 남긴 근거를 JSON 값에 적었다. (3) `check_confusing_item`이 `findWasteItems`를 직접 불러 게이트를 우회하던 것을 막았다(`소파 커버` → "1. 소파 / 결론: 대형폐기물로 신고한 뒤 배출합니다"). `modifier_fragment`는 덜 특정된 질의 신호라 그대로 둔다. (4) 측정 스크립트의 canary가 실패해도 exit 0이던 것을 `process.exit(1)`로 끊고, smoke에 없던 canary 4건(`소파 살까요?`·`우산살 버리는 법`·`샤워기 호스`·`침대 프레임 버리는 법`)을 mcp-answer-cases로 옮겼다. 지금 측정은 330 품목 × 부품어 31개 = 10,230건이고 open 갈래의 앞 품목 확정 3건, 꼬리가 등록 품목인 갈래 1,960/1,978(99.1%)이다. `뚜껑` 제거로 `냄비 유리뚜껑 칼날`·`냄비 유리뚜껑 채반` 2건이 꼬리 품목 대신 냄비로 넘어갔다 — 부품어를 기계적으로 이어 붙인 조합이라 실제 발화는 아니지만 수치에는 남는다. 2라운드(2026-08-22)에서 세 가지를 더 고치고 6건을 더했다 — (1) 이름이 어절 중간에서 끝나면 남은 꼬리만 보고 다음 어절을 안 봐서 `용`·`의` 한 글자만 끼면 게이트가 통째로 풀렸다(`모니터용 받침대`→모니터 96점, `프린터용 케이블`→프린터, `믹서기의 칼날`→믹서기). 남은 꼬리가 있으면 무조건 다음 어절까지 보게 하면 `이불이랑 커버`가 깨지므로, 뒤에 오는 말이 앞말을 **수식**하는지 앞말과 **나열**되는지로 가른다. 용도 접미사 `용`과 관형격 조사 `의`만 다음 어절로 넘어가고 조사·어미는 거기서 끊는다. (2) 게이트에 걸린 히트를 부르는 쪽마다 거르게 뒀더니 `findBestWasteItem`이 빠뜨려 `소파 커버`에 `resolveWasteItem`은 not_found, `findBestWasteItem`은 소파(36점)를 내놓고 있었다. 거르는 자리를 `findWasteItems` 한 곳으로 모으고 `check_confusing_item`의 필터를 걷어냈다 — 오타 티어로 내려갈지는 거르기 전 목록으로 정해야 해서 판정 뒤에 건다. (3) `호스`가 접미로 이미 덮는 `배수호스`를 뺐고, 정규화 동일성만 보던 validate의 중복 검사를 다른 항목의 접미로 덮이는 항목까지 잡도록 넓혔다. 부품어가 31개에서 30개로 줄어 측정 모수는 9,900건이 됐고, open 갈래의 앞 품목 확정 3건과 꼬리 품목 갈래 1,960/1,978(99.1%)은 그대로다. 3라운드(2026-08-22)에서 네 가지를 더 고치고 10건을 더했다 — (1) 붙여 쓴 복합어가 자기 머리 낱말에 걸렸다. `extendNameOccurrence`가 어절 첫머리에서만 이어 붙인 탓에 `전선케이블`이 `전선`에서 멈춰 뒤의 `케이블`을 남의 부품으로 읽었고, 같은 품목(`cable_wire`)의 이름인데도 not_found로 떨어졌다(`마우스충전기`·`소쿠리채반`·`이어폰줄케이블`도 같은 자리다). 띄어 쓴 형태는 멀쩡한데 붙여 쓰면 답이 사라지는 셈이라 어절 경계 제한을 걷어냈다 — 이어 붙이는 조건이 "같은 품목의 다른 이름이 그 자리에서 **정확히** 시작한다"라 조사나 어미로 볼 수 없는 강한 신호다. 전수로 훑은 41개 조합 중 39개가 풀렸고, 남은 둘(`칼칼날`·`채망채반`)은 게이트를 꺼도 not_found라 두 글자 별칭의 단독 등장 규칙 쪽 문제다. (2) 게이트에 걸린 구간 **안에** 통째로 든 짧은 이름이 확정으로 살아남았다 — `알약 포장재 커버`가 폐의약품을 79점으로, `화분 흙 커버`가 화분을, `기름 묻은 피자박스 커버`가 폐식용유를 내보냈다. 결과가 not_found가 아니라 틀린 확정 카드라 품목 하나만 보는 판정으로는 못 잡는다. 게이트가 참/거짓만이 아니라 덮은 구간(`NameSpan`)까지 돌려주게 하고, 걸린 구간에 자기 자리가 전부 삼켜진 품목은 `findWasteItems`에서 같이 내린다. 구간 밖에 자리가 하나라도 있으면(`믹서기 칼날`의 `칼날`, `노트북 충전기`의 `충전기`) 따로 부른 것이라 그대로 둔다. (3) 오타 티어가 게이트를 통째로 우회했다. `냉장고 야채칸`은 not_found인데 `냉장구 야채칸`은 냉장고 확정(fuzzy_jamo 70)이라 대형가전 무상방문수거 카드가 나갔다 — 제대로 쓴 쪽이 오히려 안전한 답을 받는 뒤집힌 상태였다. 오타 히트에도 같은 판정을 걸되 이름이 없어 자리를 못 짚으니, 질의에 부품어로 끝나는 어절이 있으면 오타로 짚은 이름도 그 부품어로 끝나야 통과시킨다. `정수끼 필터`→정수기 필터는 그대로 살고, 부품어 어절이 없는 `형광능`→형광등은 손대지 않는다. (4) 부품어 개수 주석이 낡아 30개인데 31개라고 적혀 있었고, 뚜껑 언급 품목도 실측 34개인데 33개였다. 측정은 open 갈래의 다른 품목 확정이 170건에서 26건으로, 꼬리가 등록 품목인 갈래가 1,960/1,978에서 1,976/1,978(99.9%)로 좋아졌다. 앞 품목 확정 3건(`CD/DVD 케이스`·`깨진 보온병 유리`·`가위 날개`)과 발화 내성 3,300건 100%는 그대로다. 남은 26건은 `아이스크림 통 커버`처럼 어절 중간에 우연히 박힌 이름(`크림통`)이라 구간으로 견줄 자리가 없는 24건에 `칫솔 손잡이`·`돌침대 프레임` 둘이다. 조건 라벨 한글화(2026-08-25)에서 6건을 더했다 — `conditionLabels`에 없는 조건 20종이 카드의 "판단 조건" 줄에 영문 키로 새고 있었다(`반려동물 배설물 묻은 휴지`가 "pet waste", `머리빗`이 "plastic material, wood or composite"). 매핑을 `condition-labels.json`으로 빼고 validate가 전수 대응을 강제한다. `자가주사기 주사바늘`의 `region_specific`은 라벨을 붙이는 대신 뺐다 — 같은 카드가 이미 `판단 범위`와 `지역 확인 필요` 블록으로 같은 말을 하고, 조건 이름이 `regionPolicy.scope`의 값(이 품목은 `local_collection_point`)과 겹쳐 읽는 사람을 헷갈리게 한다. 케이스는 라벨이 실제 응답에 실리는지 5건과 조건이 빠졌는지 1건이다. 리뷰 3라운드에서 `리빙박스·플라스틱 수납함`의 태그도 바로잡았다 — "플라스틱류"와 "복합재질"을 한 줄에서 동시에 단언하고 있었는데, 이 품목의 `checkItems`가 이미 "플라스틱 단일재질 여부"·"재활용 수거함에 들어가는 크기인지 여부"라고 적고 있어 `large_item`·`plastic`을 `bulky`·`plastic_material`로 옮겼다. 하나만 쓰던 `large_item`은 라벨과 함께 걷어냈다. 게이트-폴백 재추론 차단(2026-08-26)에서 1건을 더하고 기존 4건에 exclude를 보강했다 — 부품어·가스레인지 게이트가 not_found로 보낸 질의를 재질 폴백이 부분 문자열로 다시 훑어 방금 막은 본체를 재질 원칙으로 되살렸다("소파 커버"에 대형폐기물 신고 안내). 게이트 판정을 `inferMaterialCategories`에서 재사용해 막고, `받침대`가 `침대`로 읽히던 오인은 lookbehind로 막았다. PR #79 리뷰 2라운드(2026-08-26)에서 그 차단 범위를 좁혔다 — 처음에는 게이트가 발동하면 질의를 통째로 버렸는데, 그러면 사용자가 직접 댄 재질까지 같이 죽어 `비닐 소파 커버`·`플라스틱 소파 커버`가 재질 메뉴로 떨어졌다. 지금은 게이트에 걸린 이름만 원문에서 지우고 남은 말을 훑는다. 추정 갈래 자체는 evaluate-data의 유닛 표 11건으로 고정한다. 짧은 명사 별칭 보강(2026-08-26)에서 answer 케이스 5건을 더했다 — 한국어는 핵심어가 뒤에 와서 `향수`·`화장품`·`식세기`·`와인병`처럼 이름의 앞부분만 대면 `modifier_fragment`로 떨어져 not_found였다(`화장품`은 화장솜으로 되묻고 있었다). 이름이 이미 있는 품목에 구어 표기를 별칭으로 붙여 확정시키고, 별칭을 뺏기지 않는 반례(`화장솜`)를 함께 고정했다. 배출 경로가 다른 `아령`(고철)·`붙박이장`(공사장폐기물 경계)은 별칭으로 붙이지 않았다 — 별도 품목이 필요하다. 송파구 수수료 전량 수집(2026-08-26)에서 18행 10품목 → **156행 75품목**으로 넓혔다 — `full` 티어인데 소파·장롱·책상·책장·침대틀·세탁기·냉장고·TV가 통째로 비어 있었고, 대형폐기물 품목 107개 중 97개(91%)에 금액을 못 답하고 있었다. 조례 트랙은 좌우 2단 조판이라 「TV / 가스 레인지 = 0원」류가 섞여 못 쓰고, 스마트클린 송파(구청 표) 275행이 `smartclean` 파서로 그대로 읽힌다 — `fetch-district-fees.mjs`의 `TARGETS`에 한 줄 더한 게 전부다. 케이스는 규격 문구가 바뀐 매트리스 1건을 갱신하고(`1인용 매트리스` → `1인용 매트리스(침대틀 별도)`) 새로 덮인 소파·장롱·세탁기 3건을 더했다. 이 확장이 가능했던 건 같은 날 골든셋을 운영 데이터에서 픽스처로 떼어 냈기 때문이다 — 그전에는 송파를 채우는 순간 조례 파서 검증이 죽었다. 같은 날 서초구도 조례 별표(시행 2024-12-19) 258행으로 갈아 18행 10품목 → **122행 67품목**이 됐다. 미뤄 뒀던 이유 둘이 다 풀렸다 — 골든셋은 픽스처로 남았고, "수기 요가매트·리빙박스가 조용히 사라진다"던 두 행은 **원래 배정이 틀린 것**이었다(「대자리(돗자리)」를 요가매트로, 「서랍장」을 리빙박스로 적어 뒀다). 임포터는 서랍장을 `drawer_chest`로 제자리에 넣고 돗자리 행은 버린다 — 마포에서 세운 "틀린 값보다는 없는 편이 맞다"와 같은 판단이다. 조례가 소파를 「(응접세트)」로 적어 괄호만 남는 5행은 아직 못 읽어 `post-finals-backlog.md` 12번에 올렸다. 같은 라운드에서 공백만 든 지역(`"   "`)이 응답으로 새지 않는지 보는 케이스 1건을 더했다 — `get_disposal_steps`·`classify_waste_item`이 그 값을 다듬지 않고 그대로 써서 `-     기준 수거함 위치…` 줄과 structuredContent의 `region`에 공백이 실렸다.
- Runtime Answer Policy: 2026-07-05 지역 확인 과노출 피드백 이후 `region_review_needed`를 답변 노출 강도와 분리했다. 런타임은 `지역 영향: 필수 / 참고 / 낮음`과 `판단 범위`/`regionGuidance`를 함께 사용하며, 전용 수거함·지정 수거처·대형폐기물·불연성/특수규격 봉투·가스/유해폐기물은 강한 `지역 확인 필요`를 유지하고, 우산 같은 일반 `region_specific` 품목은 짧은 `지역 참고`만 노출한다. 구조화 응답도 참고 품목의 지역 sources를 비워 모델이 지역 확인을 과하게 키우지 않도록 했다. `get_region_disposal_info`는 품목이 있으면 지역 전체 체크리스트 대신 품목별 checkItems/item guide/수수료 후보 중심으로 확인 항목을 좁힌다.
- PlayMCP Deploy: 최신 원격 배포는 `recycle-helper-mcp` 서버 ID `1498`, endpoint `https://recycle-helper-mcp.playmcp-endpoint.kakaocloud.io/mcp`다. 최종 반영 커밋은 `409555a Limit regional structured details`이며, `tools/list` 5개, `playmcp.kakaocloud.io`/`playmcp.kakao.com` CORS, 우산 `지역 참고`, 폐건전지 `지역 확인 필요` 샘플을 확인했다.
- User decisions: 폐의약품 포장재는 별도 품목으로 승격, `공유기`/`휴대용 선풍기`는 `small_electronics` 계열로 흡수, 서초/송파 대형폐기물 수수료는 Top 품목부터 구조화한다. `비닐약봉지`는 비닐류, `비닐장판`/장판류는 일반 비닐류로 분류하지 않고 조각 종량제봉투/큰 장판 대형폐기물로 안내, `캔버스 액자`는 천과 프레임을 분리해 일반쓰레기이되 큰 프레임은 대형생활폐기물, 스탠드형 옷걸이/행거는 대형폐기물로 결정됐다. 컵라면 용기는 재질/오염 조건을 나누며, 공식 `종이 컵라면` 근거상 깨끗하면 기타 종이류, 코팅·오염·재질 불명은 일반쓰레기로 보수 안내한다. 신규 지역은 `경기도 성남시/판교`를 먼저 보강했고, `부산 해운대구` 위치형 탐색은 진행하지 않는다. 오피스텔 같은 세부 건물유형은 별도 모델링하지 않으며, 대형폐기물 수수료 추가 구조화는 당분간 중단한다.
- Source Data Modeling: `empty_medicine_bottle`, `pill_blister_packaging` 추가, `small_electronics`에 공유기/휴대용 선풍기 보강. 약 자체 `medicine`은 알약·물약·한약·연고 공식 근거를 보강했고, 폐의약품 포장재와 분리된 상태를 유지한다. 2026-07-05에는 `vinyl_flooring`, `canvas_frame`, `standing_clothes_hanger`를 신규 추가하고 `snack_bag`, `cup_noodle_container` 조건/source를 보강했다.
- Data Decision Backlog: 2026-07-05 기준 열린 결정은 없다. `부산 해운대구` 위치형 탐색은 진행하지 않고, `강남구 오피스텔` 세부 모델링과 신규 대형폐기물 수수료 구조화도 당분간 진행하지 않는 것으로 결정됐다.

## Suggested Next Split

- Source Gap: 사용자 결정분 데이터 보강은 2026-07-05 현재 완료됐다. 다음 Source 작업은 새 사용자 질문 로그나 자동 백로그에서 직접 공식 근거 보강 후보가 명확할 때만 잡고, 억지 후보는 만들지 않는다.
- Region Data: 성남시/판교 기본 정책 보강은 완료됐고, 부산 해운대구 위치형 탐색과 오피스텔/건물유형 세분화는 진행하지 않는다. 신규 대형폐기물 수수료 금액 구조화는 **2026-08-15에 재개했다** — 자치법규 ETL은 [Phase 6](prd/phase-6-bulky-fee-etl.md)이 10곳, 공공데이터포털 표준데이터 트랙이 용산·노원·강서·관악 4곳을 맡는다. 다음 지역 작업은 새 사용자 결정이나 새 지역 질문 로그가 생길 때 잡는다.
- Answer Quality: Source Gap 2026-07-05 반영분 smoke 동기화와 백로그 위생 정리는 완료됐다. 다음 Answer 작업은 새 데이터 추가 후 회귀셋 동기화 또는 새 사용자 질문 로그가 생길 때 진행한다.
