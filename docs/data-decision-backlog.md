# Data Decision Backlog

이 문서는 데이터셋 설계에서 애매한 판단이 필요한 항목을 모아두는 큐다. 매번 사용자에게 바로 묻지 않고, 열린 결정이 10건 이상 쌓였을 때 조율 세션에서 한 번에 확인한다.

## Policy

- 대상: 품목을 별도 항목으로 둘지 별칭/조건으로 흡수할지, 지역/수수료 데이터를 어느 범위까지 구조화할지 같은 데이터 모델 판단.
- 기록 방식: 열린 결정은 `Open Decisions`에 추가하고, 출처/후보/추천안을 함께 적는다.
- 사용자 확인 시점: 열린 결정이 10건 이상이면 조율 세션이 사용자에게 묶어서 확인한다.
- 예외: 원격 push/PR/배포, 큰 리팩터링, 위험한 삭제, 보안/개인정보/운영 영향이 있는 결정은 10건을 기다리지 않고 즉시 확인한다.
- 결정이 내려진 항목은 `Resolved Decisions`로 옮기고, 후속 작업 세션에 배정한다.

## Open Decisions

| Date | Decision Needed | Context | Recommendation |
| --- | --- | --- | --- |
| 2026-07-02 | `비닐약봉지`를 별도 품목으로 둘지, 기존 비닐류/폐의약품 포장 조건으로 처리할지 결정 | 생활폐기물 분리배출 누리집 품목사전에는 `비닐약봉지` 직접 항목이 있으며, 알약은 폐의약품으로 분리하고 빈 비닐약봉지는 비닐류 수거함으로 배출하도록 안내한다. 이번 사용자 결정은 `빈 약통`, `알약 포장재` 별도 품목화까지만 포함했다. | 사용자 질문 로그가 더 쌓이기 전까지는 새 품목으로 바로 추가하지 않고, `알약 포장재` 주의사항과 향후 Answer Quality 회귀에서 혼동 여부를 관찰한다. |
| 2026-07-02 | `비닐장판`을 별도 품목으로 둘지, 기존 비닐류 과매칭 방어 조건으로만 처리할지 결정 | 생활폐기물 분리배출 누리집 품목사전에는 `비닐장판` 직접 항목이 있으며, 대형폐기물 배출과 조각 형태 종량제봉투 배출을 함께 안내한다. PVC 재질이라 일반 비닐과 함께 배출하면 안 된다는 설명도 있어 기존 `비닐류` 품목으로 흡수하면 사용자가 오분류할 가능성이 크다. 다만 대형폐기물 수수료/공사장 생활폐기물/조각 크기 기준은 지역 정책과 연결될 수 있다. | 다음 데이터 보강 라운드에서 별도 `vinyl_flooring` 계열 품목으로 추가하는 쪽을 우선 검토한다. 단, 지역별 대형폐기물 수수료까지 구조화할지 여부는 Region Data 트랙 범위와 함께 묶어 결정한다. |
| 2026-07-02 | `캔버스 액자`를 `canvas_frame` 단일 품목으로 둘지, `캔버스`와 `액자`를 분리해 모델링할지 결정 | 생활폐기물 분리배출 누리집 품목사전에는 `캔버스`와 `액자`가 각각 직접 항목으로 있다. `캔버스`는 종량제봉투 또는 크면 대형폐기물, `액자`는 금속 프레임/목재·복합재질/대형/유리 분기가 갈린다. backlog 질문은 `캔버스 액자는 캔류야?`로 짧은 `캔` 과매칭 방어 가치도 있지만, 실제 배출 답변은 캔버스와 액자 재질 분기를 함께 다뤄야 한다. | Answer Quality 트랙에서 `캔` 과매칭 방어를 먼저 보강하고, 데이터 모델은 `canvas_frame` 통합 품목으로 시작할지 `canvas`와 `picture_frame`을 분리할지 다음 묶음 결정에서 정한다. 초기 추천은 사용자 질문 표현에 맞춘 `canvas_frame` 통합 품목 + 내부 재질/크기 조건이다. |
| 2026-07-02 | `부산 해운대구`를 지역 정책 데이터에 추가할지, 폐건전지 수거함 위치만 조사 후보로 둘지 결정 | 해운대구청 생활쓰레기 배출 안내는 폐형광등·폐건전지를 동사무소와 대형마트에 비치된 전용수거함에 수시배출한다고 안내하고, 공동주택은 개별단지별 차이가 있어 관리사무소 확인을 요구한다. 같은 페이지에는 자원순환과 문의처와 동 행정복지센터 링크가 있으나, 현재 확인 범위에서는 정확한 폐건전지 수거함 주소 목록을 별도 데이터셋으로 확인하지 못했다. 조사 메모: `docs/region-gap-research-2026-07-02.md` | 현재 런타임 지역이 서울 강남·서초·송파 중심이므로 해운대구를 즉시 추가하지 않는다. 정확한 위치 목록을 답하려면 해운대구 전용 위치표, 부산시 공공데이터, 또는 동별 수거함 위치처럼 주소 단위 공식 출처를 먼저 확보한 뒤 신규 지역과 `dropOffPoints` 성격의 위치 데이터를 함께 모델링한다. |
| 2026-07-02 | `강남구 오피스텔`을 별도 주거유형/건물유형 조건으로 모델링할지 결정 | 강남구청 쓰레기배출안내는 비닐·투명페트병 목요일 배출제를 단독주택, 연립주택, 빌라, 소규모 상가 등 공공 수거 배출 주체 대상으로 설명한다. 같은 페이지에는 공동주택과 연면적 1,000㎡ 이상 건물은 배출시간 및 장소를 별도 지정해 자체 처리한다고 되어 있고, `오피스텔`이라는 표현은 확인되지 않았다. 조사 메모: `docs/region-gap-research-2026-07-02.md` | 오피스텔 하나만 예외 필드로 추가하지 않는다. 유사 질문이 더 쌓이면 `buildingType`, `publicCollectionSubject`, `selfManagedCollection` 같은 넓은 조건으로 모델링하고, 그 전까지는 관리사무소 또는 자체 재활용 계약 여부 확인을 요구하는 보수적 답변 정책으로 둔다. |
| 2026-07-02 | 스탠드형 옷걸이/행거 수수료를 기존 `clothes_hanger`에 흡수할지, 별도 `standing_hanger`/`clothes_rack` 계열로 둘지 결정 | 다음 대형폐기물 수수료 후보 감사에서 강남·서초·송파 수수료표에 행거/스탠드형 옷걸이류로 보이는 행이 확인됐지만, 현재 `clothes_hanger`는 세탁소 옷걸이·철제/플라스틱 옷걸이 같은 소형 생활용품 분리배출 품목에 가깝다. 대형 행거 수수료를 같은 itemId에 붙이면 일반 옷걸이 질문이 대형폐기물로 과매칭될 수 있다. 조사 메모: `docs/region-fee-coverage-2026-07-02.md` | 기존 `clothes_hanger`에 바로 수수료를 붙이지 않는다. 사용자 질문 로그에서 `행거`, `스탠드 옷걸이`, `옷걸이` 표현이 더 쌓이면 대형 행거는 별도 itemId로 분리하고, Answer Quality에서 일반 옷걸이와 과매칭 방어 smoke를 함께 추가한다. |
| 2026-07-02 | 일반 가방류 수수료 후보를 기존 `nonwoven_bag`에 흡수할지, 별도 `bag` 계열로 모델링할지 결정 | 다음 대형폐기물 수수료 후보 감사에서 일부 수수료표의 가방류 후보가 보였지만, 현재 데이터의 `nonwoven_bag`는 부직포 장바구니/재질별 일반 배출 안내에 초점이 있다. 수수료표의 가방류가 여행가방, 캐리어, 큰 가방, 일반 가방 중 어느 범위인지 지역별로 달라 기존 `nonwoven_bag`에 붙이면 의미가 과하게 넓어진다. 조사 메모: `docs/region-fee-coverage-2026-07-02.md` | `nonwoven_bag`에 대형폐기물 수수료를 바로 붙이지 않는다. 공식 표에서 여행가방/캐리어/가방류 명칭과 규격이 분리되는지 더 확인한 뒤, 필요하면 별도 `bag` 또는 `suitcase` 계열 itemId로 모델링한다. |
| 2026-07-02 | `cup_noodle_container`를 종이 컵라면 용기와 스티로폼/발포 용기 조건으로 유지할지, 별도 품목으로 분리할지 결정 | Source Gap 재감사에서 생활폐기물 분리배출 누리집 품목사전의 `종이 컵라면` 직접 근거가 확인됐다. 다만 현재 `cup_noodle_container` item은 `styrofoam_or_paper` 성격으로 종이 컵라면 용기와 스티로폼/발포 용기, 오염 조건을 함께 다루고 있어 종이 용기 근거만 바로 source에 붙이면 item 전체를 좁게 뒷받침할 수 있다. 조사 메모: `docs/source-top50-coverage-audit-2026-07-02.md` | 즉시 source 보강하지 않는다. 다음 데이터 모델링 묶음에서 `paper_cup_noodle_container`와 `styrofoam_cup_noodle_container`를 분리할지, 기존 item에 재질 조건을 명확히 두고 공식 근거를 조건별 source로 붙일지 결정한다. |

## Resolved Decisions

| Date | Decision | Follow-up |
| --- | --- | --- |
| 2026-07-02 | 폐의약품 포장재는 약 자체와 다르므로 `빈 약통`, `알약 포장재`를 별도 품목으로 모델링한다. | Source Gap 트랙에서 공식 근거 기반 데이터 승격 후 Answer Quality 트랙에서 smoke/backlog 동기화 |
| 2026-07-02 | `공유기`, `휴대용 선풍기`는 개별 품목보다 `small_electronics` 계열 별칭/조건으로 흡수한다. | Source Gap 트랙에서 기존 소형가전 품목 별칭과 근거 보강 후 Answer Quality 트랙에서 회귀 케이스 동기화 |
| 2026-07-02 | 서초/송파 대형폐기물 수수료는 전체 표가 아니라 Top 품목 일부부터 구조화하고 이후 범위를 넓힌다. | Region Data 트랙에서 Top 품목만 우선 수집 |
