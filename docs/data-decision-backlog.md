# Data Decision Backlog

이 문서는 데이터셋 설계에서 애매한 판단이 필요한 항목을 모아두는 큐다. 매번 사용자에게 바로 묻지 않고, 열린 결정이 10건 이상 쌓였을 때 조율 세션에서 한 번에 확인한다.

## Policy

- 대상: 품목을 별도 항목으로 둘지 별칭/조건으로 흡수할지, 지역/수수료 데이터를 어느 범위까지 구조화할지 같은 데이터 모델 판단.
- 기록 방식: 열린 결정은 `Open Decisions`에 추가하고, 출처/후보/추천안을 함께 적는다.
- 사용자 확인 시점: 열린 결정이 10건 이상이면 조율 세션이 사용자에게 묶어서 확인한다. 10건 미만이고 작업을 막지 않는 낮은 시급도 결정은 사용자에게 바로 묻지 않는다.
- 즉시 질문 대상: 원격 push/PR/배포, 큰 리팩터링, 위험한 삭제, 보안/개인정보/운영 영향, 제품 범위·데이터 모델을 크게 바꾸는 결정, 또는 합리적인 기본값 없이 다음 작업이 막히는 결정.
- 낮은 시급도 처리: 품목 분리/별칭 흡수, 지역·수수료 세부 범위, 출처가 애매한 후보처럼 진행을 막지 않는 판단은 `Open Decisions`에 쌓고 추천안을 남긴다.
- 결정이 내려진 항목은 `Resolved Decisions`로 옮기고, 후속 작업 세션에 배정한다.
- 방향은 정해졌는데 마감·프리징 때문에 이번 사이클에 착수할 수 없는 항목은 [post-finals-backlog.md](post-finals-backlog.md)에도 올려 한곳에서 보이게 한다.

## 경기 6개 시 — 2026-08-16 전부 등록

인구 순으로 수원·용인·고양·화성·부천·남양주를 잡았고, **여섯 곳 모두 완결 조건 5종을 채워 넣었다.** 성남시까지 더해 경기 등록 지역은 7곳이다.

착수 전 측정한 상태가 예상보다 나빴다. 여섯 시 전부 **시 이름만으로는 `not_found`였다** — 광역 폴백조차 받지 못했다. `경기도 수원시`처럼 광역명을 통째로 붙였을 때만 경기도 광역 안내로 내려앉았는데, 카카오톡 사용자가 자기 동네를 그렇게 부를 이유가 없다. 인구 1,360만 광역이 사실상 비어 있던 셈이다.

| 지자체 | 신청 | 수수료 | 직통번호 | 어디서 찾았나 |
| --- | --- | --- | --- | --- |
| 수원시 | `waste.suwon.go.kr` 자체 시스템 | 같은 시스템의 수수료·세부요금 표 | 031-5191-3249 | 시청 생활쓰레기 배출 페이지의 담당자 정보. **시청 국번이 031-228에서 031-5191로 바뀌어** 검색 결과의 옛 번호를 그대로 쓰면 안 된다 |
| 용인시 | 시청 배출신청안내 페이지 | 시청 수수료 기준표(HTML) | 031-6193-2694 | 자체 신청 폼은 2025-03-28에 닫혔다. `BD_insertWsteExhstReqForm.do`는 아직 검색에 잡히지만 "중지된 서비스입니다"만 뜬다 |
| 고양시 | `clean.gys.or.kr` (고양도시관리공사) | 같은 사이트의 조례 별표3 표 | 031-8075-2685 | 조직도에 "대형폐기물" 담당자가 아예 없다. 스티커가 종량제봉투 업무에 붙어 있어, 시청 대형폐기물 페이지의 콘텐츠 관리부서 번호를 썼다 |
| 화성시 | 청소행정포털 `h-waste.hscity.go.kr` | 같은 페이지 검색 + 전체 PDF | 031-5189-6818 | 널리 알려진 `waste.hscity.go.kr`은 **DNS가 사라졌다.** 검색 상위에 아직 뜨는 죽은 주소다 |
| 부천시 | `clean.bucheon.go.kr` 자체 시스템 | 같은 사이트 품목별 210건 표 | 032-625-3193 | 폐건전지 안내가 메뉴가 아니라 **청소알림방 게시글**에 있었다 |
| 남양주시 | `smartclean.nyj.go.kr` 자체 시스템 | 같은 사이트 227개 품목 표 | 031-590-8280 | 옛 도메인 `cleancity.nyj.go.kr`은 접속이 끊겼는데 시청 재활용정보 페이지가 아직 그 주소를 안내한다 |

폐의약품은 이번에도 시청 메뉴 밖에 있었다. 용인은 **본문이 이미지 한 장인 시정소식 안내문**(서대문구와 같은 형태), 남양주는 **보건소 게시글의 첨부 이미지**, 부천과 고양은 보건소·새소식 게시글에서 나왔다. 서울 배치의 교훈이 그대로 재현됐다.

남은 흠 둘은 데이터에 사실대로 적었다.

- **수원 폐의약품이 두 갈래다.** 청소자원과는 정책제안 답변에서 "보건소와 각 동 행정복지센터에 수거함을 운영중"이라 했고, 2024-11 팔달구 게시글은 실제 수거함 위치를 안내한다. 그런데 수원시보건소는 2024-06 공지에서 "폐의약품을 모두 소각한다"며 약 형태별로 **소각용 종량제봉투** 배출을 안내한다. 어느 한쪽만 실으면 틀린 안내가 나가서, 수거함을 먼저 안내하고 보건소 공지를 함께 밝힌 뒤 동 행정복지센터 확인을 권하는 형태로 담았다. 시가 진행 중이라던 생활계 유해폐기물 처리계획 용역 결과가 나오면 다시 본다. **2026-08-31 교체 완료.** 정책제안 답변 URL이 죽었다(게시판 자체가 폐쇄돼 목록 페이지까지 404). `sources[1]`을 애초에 후보로 적어 둔 그 2024-11 팔달구 게시글(매산동 행정복지센터 앞 수거함 안내)로 바꿨다. 두 갈래 안내라는 사실관계는 그대로 유지한다.
- **부천 폐형광등 배출 기준을 못 찾았다.** 재활용품·생활쓰레기·대형폐기물 페이지 본문, 청소알림방 제목검색, 통합검색("폐형광등"·"형광등 수거함"·"유해폐기물")까지 훑었는데 배출 안내 문서가 없다. 조직도에 "폐형광등·건전지 운반" 담당자가 있어 수거 체계는 돌아간다. 배출 장소를 자원순환과 032-625-3212로 확인하라고 안내에 적었다.

후속 작업이 바로 쓸 수 있는 것도 남긴다. **용인·부천·고양·남양주는 품목별 수수료가 HTML 표로 열려 있어 조례 파싱 없이 임포트할 수 있다**(각각 150여 건, 210건, 조례 별표3, 227건). 수원도 HTML 표지만 인코딩이 euc-kr이라 파서에서 주의해야 한다. 화성만 검색형이라 전체 표는 PDF(`h-waste.hscity.go.kr/05_recycle/wastefee.pdf`)로 받아야 한다.

LED 조명 기준이 시마다 갈리는 것도 이번에 드러났다. 화성·고양은 평판형 등 일부 LED를 폐형광등에서 빼는데, **수원은 폐LED조명 재활용 시범사업 중이라 오히려 전용수거함으로 받고** 남양주는 형태별로 재활용 대상을 가른다. "LED는 제외"로 뭉뚱그린 안내를 전국에 돌려 쓰면 안 된다.

## Phase 5 보류 지역 — 2026-08-16 전부 해소

서울 25개 구가 모두 등록됐다. 보류했던 8개 구를 브라우저 렌더링과 게시판·첨부 탐색으로 다시 훑어 완결 조건 5종(대형폐기물 인터넷 신고 URL, 담당 직통번호, 수수료 조회 URL, 폐의약품·폐건전지 수거함 안내, 확인 출처)을 채웠다.

여덟 건 중 여섯 건은 데이터가 없었던 게 아니라 **찾는 자리가 틀렸다**. 남은 두 건은 실제로 구 단위 신청 시스템이 없어서, 사용자 결정에 따라 구청 안내 페이지를 신고 URL로 쓴다.

| 지자체 | 보류 사유였던 것 | 어디에 있었나 |
| --- | --- | --- |
| 서울 서대문구 | 폐의약품 페이지가 JS 렌더링이라 서버 응답이 비었다 | JS가 아니라 **본문이 이미지 한 장**이었다. 이미지를 읽어 배출 장소·약 형태별 방법을 확보했다. |
| 서울 구로구 | 재활용 페이지에 수거함 현황 xlsx만 있다 | 폐형광등은 `쓰레기배출요령`(key=2694) 본문에, 폐건전지는 **수거함 일제 정비 보도자료**에 배출 방법이 있었다. |
| 서울 성북구 | 구청·스마트클린 어디에도 안내 페이지가 없다 | 메뉴가 아니라 **청소/환경 게시판 「재활용품 분리배출」 글**에 전지류·형광등 배출요령이 있었다. |
| 서울 양천구 | 청소/위생 메뉴에 폐건전지 항목이 없다 | 메뉴에는 없지만 **「올바른 쓰레기 배출방법」 PDF 안내문**에 전지·형광등 칸이 있었다. |
| 서울 중랑구 | 단일 접수번호 없이 동별 수거업체 번호 3개뿐이다 | 중랑구청이 운영하는 **스마트클린 중랑에 걸린 02-2094-1956**이 대형폐기물 단일 창구다. 구청 대표번호(02-2094-0114)와 달라 표준 티어 금지 항목에 걸리지 않는다. |
| 서울 성동구 | 구청 자체 신청 링크가 빈 안내 페이지였다 | 지금은 살아 있다. `대형생활폐기물 배출신고 안내`(key=1670)에 신청하기 버튼과 수수료 부과기준표 팝업이 함께 있다. |
| 서울 중구 | 구 단위 수수료 페이지가 없다 | 여전히 없다. 신청은 '빼기', 수수료는 **구 폐기물 관리 조례 [별표5]**를 건다. |
| 서울 동대문구 | 신청은 '여기로' 최상위, 수수료는 파일 다운로드다 | 여전히 그렇다. 신청은 **구청 안내 페이지**(key=82), 수수료는 **구 조례 [별표1]**를 건다. |

교훈: "서버 응답에서 못 찾았다"를 "그 지자체에는 없다"로 옮겨 적으면 안 된다. 다음 지역 배치에서는 메뉴 페이지만 보지 말고 게시판 글·보도자료·첨부 안내문·이미지 본문까지 훑는다.

## Open Decisions

| Date | Decision Needed | Context | Recommendation |
| --- | --- | --- | --- |
| 2026-08-25 | `region_specific`의 배출 그룹 라벨을 배출 통 이름으로 바꿀지, `아이스팩`의 지역 확인 등급을 올릴지 | `disposalType`이 `region_specific`인 품목은 `아이스팩`·`LED등`·`라돈 침대` 셋이다. PR #76이 카드 머리말에 `- 배출 그룹:`을 새로 찍으면서 `아이스팩`에서 두 줄이 맞물리지 않는 게 드러났다 — `- 배출 그룹: 지역 확인 필요` 바로 아래에 `- 판단 범위: 기본 분리배출 판단은 전국 기준으로 안내 가능하며, 거주지 배출 기준만 따로 확인하면 됩니다.`가 온다. 위는 못 정한다고 하고 아래는 전국 기준으로 답할 수 있다고 한다. `LED등`과 `라돈 침대`는 같은 라벨이지만 등급이 `필수`라 아래 줄이 "지역 기준이 실제 배출 방법을 바꿀 수 있습니다"로 나가 어긋나지 않는다. 그래서 원인은 `아이스팩` 한 품목이 아니라 라벨 쪽에 있다 — `disposal-groups.json`의 나머지 값은 "재활용"·"대형폐기물"처럼 **어느 통에 넣는지**를 말하는데 `region_specific`만 "지역 확인 필요"라는 상태 서술이다. | 카드에서만 문구를 갈아 끼우지 않는다. 이 매핑은 위젯 캡션·`classify_waste_item`·`make_cleanup_plan`이 함께 읽어서, 한 표면만 고치면 PR #76이 없애고 있던 표면 간 어긋남을 도로 만든다. 실제 결정은 둘 중 하나다 — ① `region_specific` 라벨을 배출 통 이름으로 바꾼다, ② `아이스팩`을 `필수`로 올려 아래 줄을 맞춘다. ②는 이 품목을 참고 등급으로 둔 게 확인을 거친 판단이라([source-coverage.md](source-coverage.md)) 데이터를 뒤집는 쪽이고, 카드 한 줄 때문에 등급을 움직이면 지역 툴 유도 문장까지 함께 바뀐다. 어느 쪽이든 카드·위젯 캡션·cleanup plan 세 표면을 함께 놓고 정한다. ①을 고른다면 `make_cleanup_plan`이 not_found·모호 항목의 그룹으로 쓰는 "확인 필요"와 이름이 겹치지 않는지도 같이 본다. |
| 2026-08-21 | standard 티어의 "접수증 또는 접수번호를 부착해" 고정 문장을 지역별로 갈지 | `formatRegionItemGuide`([src/data.ts](../src/data.ts))가 standard 지역이면 무조건 "대형생활폐기물은 배출 전에 미리 신청하고 **접수증 또는 접수번호를 부착해** 배출합니다"를 붙인다. Phase 9로 들어온 부산 해운대구·부산진구는 배출 전에 붙일 것을 아예 발급하지 않는다 — 신청하면 대행업체가 현장에 나와 품목을 확인하고 그 자리에서 수수료를 받는다(해운대구는 영수증도 수거 현장에서 나온다). 그래서 같은 답변 안에서 고정 문장과 지역 `summary`가 정면으로 어긋난다. 스티커를 미리 사서 붙이는 인천 남동구 같은 곳도 있어, 한 문장으로 32곳을 덮는 구성 자체가 무리다. | 지역 데이터에 배출 전 부착물 유무를 담는 필드(예: `bulkyWaste.prePosting`: `receipt` \| `sticker` \| `none`)를 두고, 확인한 곳만 값을 채워 문장을 갈라 쓴다. 값이 없는 지역은 지금 문장을 그대로 쓰면 되므로 한꺼번에 채울 필요가 없다. **2026-08-21 해결.** 저장소 주인이 범위를 넓히기로 정해 이 PR에서 런타임까지 고쳤다. `bulkyWaste.prePosting`(`receipt`\|`sticker`\|`none`)을 두고 `formatRegionItemGuide`가 `none`일 때만 문장을 가른다. **값이 없는 지역은 기존 문장 그대로**라 조사하지 않은 30곳은 건드리지 않는다. 해운대구·부산진구를 `none`으로, 확인이 끝난 남동구를 `receipt`, 부평구를 `sticker`로 채웠다. `p9_haeundae_sofa_steps`·`p9_busanjin_sofa_steps`가 새 문장을, `p9_standard_prepost_default_gwanak`이 기본값이 안 움직였음을 고정한다. |
| 2026-08-25 | 수거함 안내를 본문에서 말하는데 근거 출처가 없는 15곳 | `officialSources` 순서를 정리하다 드러났다. 자치구 40곳 중 15개 자리에서 `specialCollections`가 "전용 수거함에 배출합니다"라고 답하는데 그 안내를 담은 출처가 하나도 없다 — 종로·광진·강북·도봉·은평·영등포·동작·강동·마포·성남(폐의약품), 송파·용산·강서·금천·종로(폐건전지·폐형광등). 순서로는 못 고치고 출처를 찾아야 한다. `smoke-mcp.mjs`의 `KNOWN_GUIDE_SOURCE_GAPS`가 이름을 들고 있고, 새 지역이 같은 상태로 들어오면 거기서 걸린다. | 지역마다 구청 안내 페이지를 찾아 채운다. 서대문·구로처럼 본문이 이미지인 게시글에 있는 경우가 있으니 서버 응답이 비어 보인다고 없다고 보지 않는다. 목록은 줄어야 하고, 다 채우면 그 상수와 예외 갈래를 함께 지운다. |
| 2026-08-15 | `가스레인지대`·`레인지후드`를 별도 품목으로 세울지 | Phase 7 후속 배치에서 `가스레인지`를 추가하자 이름이 접두어로 걸려 `가스레인지대`(받침장)·`가스레인지 받침대`·`가스레인지 후드`가 98점으로 gas_range에 단독 확정된다. 배치 전에는 not_found라 되물었다. 받침장은 가구인데 사용자는 "가스 밸브를 잠그고 연결 호스를 분리" 절차와 가스레인지 수수료를 받는다. 기존 품목에 뺏기는 형태가 아니라 대응할 품목 자체가 없어서, `복사기 토너`→토너 카트리지처럼 별칭으로 되돌릴 수가 없다. gas_range의 주의사항과 source note는 이미 둘이 다른 품목임을 적어 두고 있다. **2026-08-17에 확정만 막았다** — `resolveWasteItem`의 게이트가 `가스레인지`를 품은 채 `후드`·`받침`이 붙거나 `레인지대`로 끝나는 질의를 not_found로 돌린다(`가스렌지` 표기도 함께 모은다). 잘못된 안내는 멎었지만 받침장·후드를 물은 사용자는 여전히 되묻기만 받으므로, 품목을 세울지는 그대로 열린 결정이다. | 표준데이터에 `가스레인지대`·`레인지후드` 고시 행이 있는지 먼저 확인하고, 있으면 품목사전 근거를 붙여 별도 품목으로 세운 뒤 담당 4곳을 재임포트한다. 근거 없이 찬장 같은 인접 가구 품목의 별칭으로 흡수하는 방식은 쓰지 않는다 — 사용자가 물어본 물건과 다른 이름의 카드가 나간다. 품목을 세울 때는 게이트에서 먼저 빼야 한다. `evaluate-data.ts`의 예약어 검사가 그 순서를 강제한다. |
| 2026-08-21 | 두 글자 품목 이름 `벨트`·`젖병`에 수식어 게이트를 넣을지, 전동 벨트를 별도 품목으로 세울지 | Phase 8에서 `belt`를 추가하자 이름이 짧은 별칭으로 걸려 `안마 벨트`·`벨트 마사지기`·`복대 벨트`·`등산 벨트`가 79점으로 belt에 단독 확정된다. 추가 전에는 not_found라 재질 폴백을 받았다. 앞의 둘은 배터리나 전원을 쓰는 소형가전인데 사용자는 "물기에 젖지 않게 담아 의류수거함으로" 안내를 받는다. `러닝머신 벨트`(97점 `exercise_machine`)처럼 뺏어 갈 품목이 있는 경우는 이미 막혀 있고, 여기 걸린 넷은 대응 품목 자체가 없어 별칭으로 되돌릴 수가 없다. `가스레인지대`가 같은 이유로 이 표에 올라와 있다. 다만 게이트 방식은 그쪽보다 위험하다 — `가스레인지`는 다섯 글자라 접두어 규칙이 좁게 먹지만, `벨트`는 두 글자라 규칙을 조금만 넓혀도 `벨트 어떻게 버려?` 같은 정상 질의가 함께 죽는다. **저장소 주인이 2026-08-21에 게이트 도입을 보류해 이번 PR에서는 코드를 건드리지 않았다.** 같은 Phase에서 넣은 `baby_bottle`도 이름이 두 글자라 똑같이 걸린다 — `젖병 소독기`는 `bottle_sterilizer`로, `젖병 솔`·`젖병 브러시`는 `dish_brush` 별칭으로 되돌렸지만, 대응 품목이 없는 `젖병 집게`·`젖병 보관함`·`젖병 케이스`·`젖병 세정제`은 79점으로 젖병 카드에 남는다. 젖병 카드는 젖꼭지를 떼고 몸체 재질을 가리라고 안내하므로, 집게와 보관함을 물은 사람에게는 맞지 않는다. 원인이 벨트와 같으니 해법도 같이 정한다. | 게이트를 서두르지 않는다. `안마기`·`마사지기` 계열 품목을 소형가전 근거로 먼저 세우면 `안마 벨트`·`벨트 마사지기`는 별칭으로 되돌릴 수 있고, 그 편이 게이트보다 사용자에게 나은 답을 준다. `복대 벨트`·`등산 벨트`는 의류수거함이 받는지부터 확인해야 해서 근거가 먼저다. 게이트를 끝내 넣는다면 `가스레인지` 전례대로 `resolveWasteItem`에 두고, 반드시 `벨트`·`가죽 벨트`·`천 벨트` 확정을 반례로 함께 고정한다(`젖병` 쪽이면 `젖병`·`아기 젖병`·`분유병`). `evaluate-data.ts`의 예약어 검사가 품목을 세울 때 게이트에서 먼저 빼도록 강제한다. |
| 2026-08-15 | 상한으로 잘린 수수료 행이 있다는 사실을 답변에 드러낼지 | `trimToCap`이 (지역, 품목)당 12행으로 줄이는데 잘렸다는 표시가 어디에도 안 남는다. 노원구 매트리스는 21행 중 9행이 빠졌고, `formatBulkyWasteFeeLines`는 남은 12행을 "수수료 후보"로 그냥 나열한다. 자기 규격이 표에 없는 사용자는 그게 원래 없는 건지 잘린 건지 알 수 없다. 임포터는 stdout에 찍지만 사용자도 회귀 케이스도 그걸 못 본다. | "전체 N종 중 12종"처럼 잘린 사실을 한 줄로 밝히는 게 맞다. `formatBulkyWasteFeeLines` 수정이라 런타임 변경이고 8/21 게이트에 걸려, Phase 6의 런타임 필터링 과제와 함께 다룬다. 그때까지는 수수료 조회 URL이 전체 표로 가는 경로를 대신한다. |
| 2026-08-15 | 소형가전 계열 품목에 대형폐기물 수수료를 노출할지 | R4 임포트로 지자체 고시에 값이 있는 품목이 90여 개로 늘면서, 우리 안내가 무상(전용수거함·재활용·종량제봉투)인 품목 30개에도 금액이 붙었다. `findBulkyWasteFees`에 배출 경로 게이트를 넣어 지금은 안 나간다. 다만 청소기·노트북·프린터·모니터처럼 `small_electronics_collection`인 품목은 실제로 지자체가 대형폐기물로도 받는 경우가 있어, 게이트가 맞는 답을 가리고 있을 수 있다. | 품목별로 확인해 대형폐기물 갈래가 실재하면 `disposalType`을 `small_electronics_collection_or_bulky`로 올린다(이미 있는 값이라 게이트가 자동으로 열린다). 근거 없이 게이트를 푸는 방식은 쓰지 않는다 — "무료로 내면 되는 물건에 수수료를 답한다"가 원래 문제였다. |
| 2026-08-15 | 텍스트 폴백 응답에서 text와 structuredContent의 지역 안내 중복을 없앨지 | Phase 7 R4 임포트 후 `WIDGET_ENABLED=false`로 `get_disposal_steps{itemName:"매트리스", region:"서울 노원구"}`를 재면 5.7KB다(text 3.4KB + structuredContent 2.3KB). 수수료 12행은 931B뿐이고, 큰 덩어리는 **같은 지역 안내가 두 번 실리는 것**이다 — structuredContent의 `regionNotes` 1,484B가 text의 `### 지역 확인 필요` 1,733B와 사실상 같은 내용이다. 위젯 경로(본선 실사용)는 요약 렌더링이라 1.5~1.8KB로 문제없다. (작성 당시. 2026-08-18부터 위젯 응답도 structuredContent를 함께 실어 지역 품목은 3.5~4.1KB — 노원구 매트리스 4,091B 실측. 카드 본문은 여전히 요약이지만 `regionNotes`는 잘리지 않고 들어가므로, 아래 중복 제거는 두 경로 모두의 이야기가 됐다. 근거는 phase-3 PRD의 "R4 결정 변경".) | 행 상한을 더 조여도 안 풀린다(수수료는 전체의 16%뿐). `regionNotes`를 빼거나 text 쪽 지역 섹션을 줄이는 쪽이 맞다. 다만 `regionNotes`를 기대하는 회귀 케이스가 있어 런타임·케이스 동시 변경이고 8/21 게이트에 걸린다. 본선 이후 과제로 두되, 여유가 생기면 Phase 6의 런타임 필터링 과제와 함께 다룬다. |
| 2026-08-14 | 재질 포괄어 질의를 되묻게 할지, 재질 안내로 답할지 | Phase 7에서 수식어 폴백 가드를 전수 탐색하다 발견했다. `종이`는 `코팅지`(91점)로, `비닐`류 일부도 특정 품목으로 단독 확정된다 — 핵심어 점수가 HIGH_CONFIDENCE_SCORE(88)를 넘으면 수식어 폴백 블록에 들어가지 않기 때문이다. "종이 어떻게 버려?"에 코팅지 안내가 나가는 셈이다. 반면 `플라스틱`·`유리`는 최고점이 86이라 되묻는다. 즉 같은 성격의 질의가 점수에 따라 갈린다. | 재질 포괄어는 품목 확정 대신 `material-guidelines.json`(Phase 1)의 재질 안내로 답하는 경로가 자연스럽다. 다만 런타임 매칭 변경이라 8/21 게이트에 걸리고, 측정 세트에 재질 포괄어가 없어 지금 지표에는 영향이 없다. 본선 이후 과제로 둔다. |
| 2026-08-14 | 공식 근거를 못 찾아 제외한 빈출 품목을 어떻게 처리할지 | Phase 2 리서치에서 플라스틱 양념통, 변기솔, 선크림 튜브, 가죽 벨트, 젖병, 사료 포대, 세탁망, 앞치마, 쓰레받기, 밀랍랩, 조화, 강아지 목줄, USB 메모리, 외장하드 등은 품목사전 미등재 + 지자체 공식 안내도 확인되지 않아 추가하지 않았다. | 재질 기준(플라스틱/섬유/복합재질) 일반 안내로 흡수할지, 지자체 품목표를 추가 조사해 개별 품목으로 올릴지 결정한다. 그 전까지는 not_found 폴백(Phase 1)에 맡긴다. |
| 2026-08-14 | 표준 티어(`standard_import`) 품목의 심층 검수 승격 기준 | Phase 2에서 142개를 단일 출처·조건 분기 최소화로 추가했고, Phase 7에서 52개가 더 붙어 지금은 194개다([source-coverage.md](source-coverage.md)). 이 중 실제 질의가 몰리는 품목은 다중 출처·조건 분기로 올릴 필요가 있다. **2026-08-19에 원래 계획이 막혔다** — 승격 순서를 본선 호출 로그에서 뽑겠다고 적어 뒀는데, PlayMCP in KC 콘솔에 컨테이너 stdout을 보여주는 화면이 없어 운영 로그에 손이 닿지 않는다([qa-runbook.md](qa-runbook.md) 2절). 로그 줄 자체는 쓸 만하다 — 확정 매칭이면 `matchedId`가 남으니([src/server.ts](../src/server.ts)) 등록된 품목의 빈도는 그대로 나온다. 막힌 건 필드가 아니라 접근이라, 콘솔에 로그 화면이 생기면 원래 계획이 그대로 살아난다. | 그때까지는 우리가 모은 질의로 순서를 정한다. **① 수집** — 문의 내용과 로컬 테스트에서 나온 질의를 `pnpm log:query -- --query "..." --region "..."`로 `logs/manual-queries.jsonl`에 쌓는다(커밋하지 않는다). qa-runbook 3절 ①과 같은 흐름이다. **② 1순위 뽑기** — `pnpm backlog:auto -- --input logs/manual-queries.jsonl --include-medium-confidence`를 dry-run으로 돌려 `source_gap` 후보를 받는다. 이 필터는 티어가 아니라 `confidence`와 `needs_source`를 보므로 다른 상태의 품목도 섞인다 — 사유에 `review.status=standard_import`가 찍힌 것만 골라낸다. `confidence`가 `medium`인 표준 티어가 여기 걸리고, 근거 보강이 가장 급한 쪽이다. **③ 나머지 순서** — `confidence`가 `high`면 `confident_match`로 걸러져 후보에 아예 안 뜬다. 이쪽은 로컬에서 서버를 띄우고(`pnpm start`) 모아 둔 질의를 그대로 쳐서 로그의 `matchedId`를 세면 순서가 나온다. backlog:auto의 Skipped 줄로는 못 센다 — 같은 질의가 `duplicate_in_input`으로 걷히면서 품목 id도 안 찍히고, 출력도 20줄에서 잘린다. **④ 승격** — 지역이 답을 가르면 `region_review_needed`, 다중 출처와 조건 분기까지 확인했으면 `verified`다. 상태를 바꿨으면 [source-coverage.md](source-coverage.md)의 티어별 개수도 같이 고쳐야 `pnpm check`가 통과한다(`validate-data.mjs`가 문서 숫자와 데이터를 대조한다). 데이터만 건드리는 승격이라 기능 변경 마감 뒤에도 할 수 있고, 재배포가 필요하니 9/14 프리징이 상한이다. |

## Resolved Decisions

| Date | Decision | Follow-up |
| --- | --- | --- |
| 2026-08-16 | `specialCollections.*.method`의 둘째 줄부터는 **물어본 품목이 걸린 쪽만** 펼친다. | `regionSpecialCollectionLines`([src/server.ts](../src/server.ts))가 `method[0]`만 싣던 것을 고쳤다. 폐의약품을 물으면 폐의약품 줄 전체가, 형광등·건전지를 물으면 그쪽 줄 전체가 나가고, 나머지 한 줄과 품목을 안 물은 경우는 예전대로 첫 줄만 나간다 — 지역 개요 응답 크기는 그대로다(성북 1,296자). 항상 전부 싣지 않은 이유는 폐의약품을 물은 사람이 형광등 예외까지 받을 이유가 없어서다. 회귀 케이스 4건으로 확장·비확장·교차 미노출을 고정했다. 살아난 내용: 성북 "평판형·십자형·원반형 LED는 특수규격봉투", 중랑 "백열전구·LED등은 특수마대", 양천 "깨진 형광등은 불연성 마대", 중구 "리튬전지는 '비대상전지' 표기". PR #34에서 첫 줄에 밀어넣었던 중복도 함께 걷어냈다. |
| 2026-08-16 | "배출 3일 전까지 사전 신청"은 기한을 직접 확인한 `full` 티어에서만 쓴다. `standard`는 기한을 빼고 신청 경로로 넘긴다. | `formatRegionItemGuide`([src/data.ts](../src/data.ts))의 분기를 `coverageTier === "full"` 기준으로 바꿨다. standard 21곳은 "배출 전에 미리 신청하고 … 신청 기한은 아래 신청 경로에서 확인하세요"로 나간다. 기존 회귀 케이스 3건은 전부 강남구(full)와 경기(metro)라 기대값을 건드리지 않았다. 지역별 실제 기한을 담으려면 `bulkyWaste.leadTimeDays`를 두고 확인한 곳만 숫자를 싣는다. |
| 2026-08-16 | 상용 대행 서비스에 신청을 위임한 구는 그 구청의 대형폐기물 안내 페이지를 `applicationUrl`로 쓴다. 상용 서비스 최상위 페이지는 그대로 인정하지 않는다. | 서울 중구(`junggu.seoul.kr/content.do?cmsid=16063`)와 동대문구(`ddm.go.kr/www/contents.do?key=82`)에 반영해 서울 25개 구를 모두 열었다. 수수료는 두 곳 다 구 폐기물 관리 조례 별표를 건다. 상용 서비스에 구별 딥링크가 생기면 그때 갈아끼운다. |
| 2026-08-14 | `노트북/랩탑`은 `small_electronics` 흡수가 아니라 별도 품목으로 둔다. | Phase 2에서 `laptop` 품목(소형가전 전용수거함 기준)과 평가/answer 케이스로 반영 완료. 기존 "랩탑" not_found smoke는 정상 매칭 기대로 갱신했다. |
| 2026-08-14 | `정수기 필터`를 신규 품목으로 추가한다. | Phase 2에서 연수필터 종량제 기준을 근거로 `water_purifier_filter` 추가 완료. 백로그 auto_b6aece0913을 `covered`로 닫았다. |
| 2026-08-14 | `햇반/즉석밥 용기`는 별칭 흡수가 아니라 별도 품목으로 둔다. | Phase 2에서 품목사전 근거(niIdx=98)로 `instant_rice_container` 추가 완료. OTHER 재질이라 지역 확인 필요로 표시했다. |
| 2026-07-02 | 폐의약품 포장재는 약 자체와 다르므로 `빈 약통`, `알약 포장재`를 별도 품목으로 모델링한다. | Source Gap 트랙에서 공식 근거 기반 데이터 승격 후 Answer Quality 트랙에서 smoke/backlog 동기화 |
| 2026-07-02 | `공유기`, `휴대용 선풍기`는 개별 품목보다 `small_electronics` 계열 별칭/조건으로 흡수한다. | Source Gap 트랙에서 기존 소형가전 품목 별칭과 근거 보강 후 Answer Quality 트랙에서 회귀 케이스 동기화 |
| 2026-07-02 | 서초/송파 대형폐기물 수수료는 전체 표가 아니라 Top 품목 일부부터 구조화하고 이후 범위를 넓힌다. | Region Data 트랙에서 Top 품목만 우선 수집 |
| 2026-07-03 | `비닐약봉지`는 별도 폐의약품 포장재 품목이 아니라 빈 봉지로 보고 비닐류로 처리한다. | 2026-07-05 Source Gap에서 `snack_bag` 별칭/source로 반영 완료. Answer Quality 트랙에서 비닐약봉지 질문 smoke를 추가한다. |
| 2026-07-03 | `비닐장판`/장판류는 일반 비닐류 재활용으로 분류하지 않는다. 조각은 종량제봉투, 큰 장판은 대형폐기물 기준으로 안내한다. | 2026-07-05 Source Gap에서 `vinyl_flooring` 별도 품목과 평가 케이스로 반영 완료. 수수료 금액 구조화는 보류하고 Answer Quality 트랙에서 `비닐류` 과매칭 방어 smoke를 유지/보강한다. |
| 2026-07-03 | `캔버스 액자`는 천과 프레임을 분리해 일반쓰레기로 버리되, 프레임이 너무 크면 대형생활폐기물 신청 대상으로 안내한다. | 2026-07-05 Source Gap에서 `canvas_frame` 통합 품목과 평가 케이스로 반영 완료. Answer Quality 트랙에서 `캔` 과매칭 방어와 크기 조건 smoke를 추가한다. |
| 2026-07-03 | 스탠드형 옷걸이/행거는 일반 옷걸이와 분리해 대형폐기물로 안내한다. | 2026-07-05 Source Gap에서 `standing_clothes_hanger` 별도 품목과 평가 케이스로 반영 완료. 수수료 금액 구조화는 보류하고 Answer Quality 트랙은 일반 옷걸이 과매칭 방어 smoke를 추가한다. |
| 2026-07-03 | 컵라면 용기는 재질별로 분리한다. 발포/스티로폼 용기는 세척 후 재활용 가능성을 확인하고, 종이 컵라면은 공식 근거상 깨끗하면 기타 종이류, 코팅·오염·재질 불명은 일반쓰레기로 보수 안내한다. | 2026-07-05 Source Gap에서 `cup_noodle_container` 조건/source 보강 완료. Answer Quality 트랙에서 종이/스티로폼 조건 smoke를 추가한다. |
| 2026-07-05 | 신규 지역은 `부산 해운대구`보다 `경기도 성남시/판교`를 먼저 보강했고, 이후 `부산 해운대구` 위치형 탐색은 진행하지 않는다. | 성남시/판교 기본 정책은 1차 반영 완료. 해운대구 폐건전지 수거함 위치형 질문은 제품 범위에서 제외해 백로그 `wont_fix`로 닫고, 미지원 지역에서는 주소를 지어내지 않는 smoke만 유지한다. |
| 2026-07-05 | `강남구 오피스텔` 같은 세부 건물유형은 당분간 별도 모델링하지 않는다. | Region Data/Answer Quality 트랙은 오피스텔 전용 조건을 추가하지 않고, 공동주택·관리사무소·자체 수거계약 확인이 필요한 경우 일반적인 보수 안내로 처리한다. |
| 2026-07-05 | 대형폐기물 수수료 추가 구조화는 당분간 중단한다. | 지역별 세부 기준 차이가 커서 새 수수료 itemId 연결은 보류한다. 이미 구조화된 수수료 데이터는 유지하되, 신규 품목은 대형폐기물 신청 대상 여부까지만 안내하고 금액 구조화는 하지 않는다. **2026-08-15 이 결정을 되돌렸다** — 중단 사유가 수작업 비용이었는데 법제처 자치법규 API와 공공데이터포털 표준데이터로 자동 수집이 가능해졌다. 재개 범위는 이미 등록된 지역과 기존 대형폐기물 품목으로 못박는다. [Phase 6 PRD](prd/phase-6-bulky-fee-etl.md) 참조. |
| 2026-08-31 | 표준데이터 트랙이 넣은 `feeKrw: 0` 행(용산 12·노원 11, 총 23행)은 지우지 않고 "무상"으로 표기한다. | 저장소 주인이 결정했다. `formatKrw`([src/data.ts](../src/data.ts))가 `feeKrw === 0`이면 "N원" 대신 "무상"을 찍도록 고쳤고, `buildRegionFeeLine`([src/server.ts](../src/server.ts))에 따로 있던 같은 조립 로직을 걷어내고 `formatKrw`를 쓰게 했다. 두 함수를 하나로 모은 덕에 텍스트 응답·위젯 카드·`make_cleanup_plan`이 한 번에 바뀐다. 데이터 행 자체는 손대지 않았다. 회귀 케이스로 용산구 전자레인지가 "모든규격 무상"으로 나오는지 스모크에 고정했다(`bulky_fee_free_row_yongsan_microwave`). |
