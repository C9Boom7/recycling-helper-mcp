# 뒤에 슬래시를 붙이면 디렉터리만 걸러진다. 워크트리는 node_modules를 메인
# 저장소로 거는 심볼릭 링크로 두는데, 슬래시가 붙어 있으면 그 링크가 무시 대상에서
# 빠져 git에 커밋돼 버린다(실제로 커밋돼 있었다).
node_modules
dist/
.pnpm-store/
.DS_Store
*.log
.env
logs/*
!logs/
!logs/README.md
!logs/*.example.jsonl
# 자치법규 별표 원문·파싱 결과 (Phase 6 R1). 검수용 중간 산출물이라 커밋하지 않는다.
data/ordinance-raw/
data/district-fee-raw/
# 에이전트 세션이 병렬 작업용으로 만드는 임시 워크트리. 같은 저장소를 한 번 더
# 체크아웃한 것이라 커밋 대상이 아니다.
.claude/worktrees/
