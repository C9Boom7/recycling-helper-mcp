apiVersion: v1
kind: Service
metadata:
  name: recycling-helper-mcp-service
  labels:
    app: recycling-helper-mcp
spec:
  type: LoadBalancer
  selector:
    app: recycling-helper-mcp
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000

